---
date: 2024-10-18
tags:
  - AdML
  - uris
  - polyu
---
# 第8章 攻击与防御深度学习

近年来，深度学习在多个领域中取得了显著的成功，包括计算机视觉和自然语言处理等应用。然而，随之而来的是==深度神经网络模型对输入的微小对抗性变化表现出的脆弱性。==这些变化最初被视为鲁棒性测试，而非实际攻击，但==随着研究的深入，对抗性攻击的语言逐渐被更字面地理解，尤其与安全和可靠性应用相关。==

### 深度学习的脆弱性

深度学习的对抗性问题与早期的对抗性机器学习研究几乎独立发展，但同样引起了广泛的关注。本章专注于==深度学习的攻击与防御==

### 深度学习模型简介

本章首先简要描述典型的深度学习模型，接着讨论对深度神经网络的决策时攻击。==我们将这些攻击框定为一般的优化问题==，并根据量化扰动成本的距离度量将攻击分为三大类：

1. **$l_2$-范数攻击**：攻击者旨在==最小化对抗图像与原始图像之间的平方误差==，通常导致图像中添加非常少量的噪声。
   
2. **$l_1$-范数攻击**：这一类攻击的目标是==限制或最小化任何像素的扰动量==，以实现攻击者的目标。

3. **$l_0$-范数攻击**：这些攻击旨在==最小化图像中被修改的像素数量==。

在讨论了数字图像的通用攻击方法后，我们还介绍了如何在物理世界中使这些攻击成为现实的方法，即对物理对象进行对抗性修改，使其在数字处理后被错误分类。

### 深度学习的防御方法

随着对深度学习的攻击方法的出现，针对这些攻击的防御方法也逐渐增多。我们介绍三种保护深度学习免受决策时攻击的通用方法：

1. **鲁棒优化**：这是最具==理论基础的方法==，旨在将鲁棒性直接嵌入学习过程中，从而在原则上保证或认证鲁棒性。

2. **再训练**：我们在第5章中讨论的==迭代再训练方法==可以直接应用于提高深度学习的鲁棒性。

3. **蒸馏**：这是一种启发式方法，==通过有效地重新缩放输出函数使得基于梯度的攻击更难以执行，从而使梯度变得不稳定==。值得注意的是，蒸馏可能会被最新的攻击方法击败。

## 8.1 神经网络模型 NEURAL NETWORK MODELS

深度学习依赖于神经网络模型，==通过多层非线性变换将输入特征向量转化为最终输出==。分类任务的==输出是各类的概率分布，而回归任务则提供实值预测==。深度学习的“深”之处在于其包含==多个变换层，每层可以执行不同的功能==。

### 深度神经网络的构造

深度神经网络 $F(x)$ 被定义为一系列层的组合：

$$
F(x) = F_n \circ F_{n-1} \circ \ldots \circ F_1(x)
$$

每一层 $F_l(z_{l-1})$ 将前一层的输出 $z_{l-1}$ 转换为当前层的输出 $z_l$，公式为：

$$
z_l = F_l(z_{l-1}) = g(W_l z_{l-1} + b_l)
$$

这里，$W_l$ 是权重矩阵，$b_l$ 是偏置向量，$g(\cdot)$ 是非线性激活函数，如 Sigmoid 函数或 ReLU 函数。

### 分类输出

在分类任务中，神经网络的最终输出是一个概率分布 $p$，满足 $p_i \geq 0$ 且 $\sum_i p_i = 1$。最后一层通常采用 softmax 函数来生成这一概率分布，其计算公式为：

$$
p_i = F_i(x) = \text{softmax}(Z(x))_i = \frac{e^{Z_i(x)}}{\sum_j e^{Z_j(x)}}
$$

### 预测分类

预测的类别 $f(x)$ 是具有最大概率的类别：

$$
f(x) = \arg \max_i F_i(x)
$$

### 对抗性环境

在对抗环境中，研==究者不仅关注网络 $F(x)$ 的概率输出，还关注倒数第二层的输出 $Z(x)$==，该层提供在 softmax 处理之前的实值输出==。这一关注点对于理解深度学习模型在遭遇对抗性攻击时的脆弱性至关重要。==

![[Pasted image 20241018104505.png]]

## 8.2 深度神经网络的攻击：对抗性示例

本节探讨了深度学习模型的攻击，首先讨论白箱攻击，其次简要介绍黑箱攻击。

### 对抗性示例的构造

对抗性攻击从==一个干净的原始图像 $x_0$ 开始，向其添加噪声 $\delta$，形成被攻击的图像 $x_0 + \delta$。==攻击的目的是导致分类错误。添加足够的噪声会使分类结果出错，因此攻击者通常会==限制噪声的大小==，使用范数约束 $\|\delta\| \leq \epsilon$，其中 $\epsilon$ 是外部设定的阈值。常用的范数包括 $l_2$、$l_1$ 和 $l_0$。

### 攻击优化问题

攻击者的优化问题有多种形式。Szegedy 等人提出的目标是==最小化添加的对抗噪声的范数，同时使图像被错误分类为目标类 $y_T$：==

$$
\min \|\delta\| \quad \text{s.t.} \quad f(x_0 + \delta) = y_T, \quad x_0 + \delta \in [0, 1]^n
$$

这种攻击称为==有针对性的攻击。相应的可靠性攻击则试图使图像被错误分类为任何其他类==。

### $l_2$ 范数攻击

在对抗性攻击中，==$l_2$ 范数攻击是有效的策略之一==。攻击者通过最小化对抗扰动的 $l_2$ 范数来实现目标。

#### Szegedy 的 $l_2$ 攻击

Szegedy 等人最早提出的针对性 $l_2$ 攻击使用代理问题来量化攻击引入的错误：

$$
\min c \|\delta\|_2 + l(F(x_0 + \delta), y_T) \quad \text{s.t.} \quad x_0 + \delta \in [0, 1]^n
$$

==这一方法可通过标准的无约束优化技术求解==。

#### Carlini 和 Wagner 的 CW 攻击

Carlini 和 Wagner 提出的 CW 攻击改进了优化算法，重新表述约束条件，使攻击更具鲁棒性。通过构造函数 $h(x_0 + \delta | y_T)$，将约束转化为目标函数的一部分：

$$
\min \|\delta\|_2 + c \cdot h(x_0 + \delta, y_T) \quad \text{s.t.} \quad x_0 + \delta \in [0, 1]^n
$$

### DeepFool 攻击

DeepFool 攻击利==用线性近似，实现可靠性攻击==。假设分类器是线性的，最优攻击在此背景下求解：

$$
\min \|\delta\|_2 \quad \text{s.t.} \quad W^T w_k (x_0 + \delta) + b_k \geq w_k (x_0 + \delta) + b_{f(x_0)}
$$

DeepFool 通过 Taylor 近似迭代地线性化分类器，寻找最小扰动以使分类结果改变。

### 总结

本节深入探讨了针对深度学习模型的对抗性攻击，特别是基于 $l_2$ 范数的攻击。这些攻击揭示了模型的脆弱性，并为制定有效的防御策略提供了背景。

## 8.2.2 $l_\infty$ 范数攻击

### FGSM 攻击

Goodfellow 等人提出的==快速梯度符号方法fast gradient sign method (（FGSM）==是针对深度学习的可靠性攻击之一，旨在通过添加噪声 $\delta$ 来诱导分类错误，同时满足 $l_\infty$ 范数约束，即 $\|\delta\|_\infty \leq \epsilon$。虽然损失最大化问题难以精确求解，FGSM 的==关键在于对损失函数进行线性化==：
==(公式有误)==
$$
l_Q(\delta) = l(F(x_0), y) + \nabla_x l(F(x_0), y) \delta
$$

对于==可靠性攻击，最佳方案是在每个坐标方向上独立地最大程度扭曲==：

$$
\delta^* = \epsilon \, \text{sgn}(\nabla_x l(F(x_0), y))
$$

对于==有针对性的攻击，则是相反方向==：

$$
\delta^* = -\epsilon \, \text{sgn}(\nabla_x l(F(x_0), y_T))
$$

==FGSM 攻击是单步梯度更新，虽然效率高，但攻击能力有限==。

### 投影梯度下降攻击（PGD）

==PGD 攻击是一种更强大的方法，通过迭代的信任区域优化，线性化目标函数，并在小的信任区域内优化==。形式化地，假设 $\beta_t$ 是更新参数，$x_t$ 是第 $t$ 次迭代的对抗图像。对于可靠性攻击，更新过程为：

$$
x_{t+1} = \text{Proj}_{\epsilon}[x_t + \beta_t \, \text{sgn}(\nabla_x l(F(x_t), y))]
$$

该函数将结果投影到满足 $x_0 - x_{t+1} \leq \epsilon$ 的空间。针对攻击的变体则将“+”替换为“-”。

### Carlini 和 Wagner 的 $l_\infty$ 攻击

Carlini 和 Wagner 提出的另一种 $l_1$ 攻击用代理替代 $\|\delta\|_1$ 项：

$$
\sum \max(0, \delta_i - c)
$$

其中 $c$ 是外部设定的常数，从 1 开始，在每次迭代中减少。如果在某次迭代中所有 $|\delta_i| \leq c$，则 $c$ 减少 0.9 倍。

## 8.2.3 $l_0$ 范数攻击

### Jacobian 基于显著性图攻击 Jacobian-based Saliency Map Attack（JSMA）

JSMA ==攻击旨在最小化修改的像素数量==，以==使图像被错误分类为特定目标类 ==$y_T$。攻击从原始图像 $x_0$ 开始，贪婪地修改像素对。修改的选择基于两个量的启发式：

- $\alpha_{ij}$：改变像素 $i$ 和 $j$ 对目标类 $y_T$ 的影响。
- $\beta_{ij}$：改变这些像素对其他类的影响。

攻击选择最大化显著性分数 $s_{ij}$ 的像素对进行修改：

$$
s_{ij} = \begin{cases}
0 & \text{if } \alpha_{ij} < 0 \text{ or } \beta_{ij} > 0 \\
-\alpha_{ij} \beta_{ij} & \text{otherwise}
\end{cases}
$$

### Carlini 和 Wagner 的 $l_0$ 攻击

Carlini 和 Wagner 的 $l_0$ 攻击利用他们的 $l_2$ 攻击作为子例程，==逐步应用 $l_2$ 攻击到图像的缩小部分。每次迭代中，排除对攻击成功影响最小的像素。==

## 8.2.4 物理世界中的攻击

### 攻击背景

本节讨论的攻击通常==假设攻击者可以直接访问数字图像并进行任意修改。然而，实际执行此类攻击往往需要对被拍摄的物理对象进行改动。==

### 物理攻击案例

1. **Sharif 等人的攻击（2016）**：
   - 该研究开发了==针对面部识别系统的攻击，攻击者佩戴印刷的眼镜框，这些框中嵌入了对抗噪声。==
   
2. **Evtimov 等人的攻击（2018）**：
   - 该研究描述了两种攻击方式：一种是打印自定义的停车标志海报，可以覆盖实际的停车标志；另一种是打印看似普通涂鸦的贴纸。

### 物理攻击的挑战

物理攻击成功实现面临三大挑战：

1. **隐蔽性**：
   - 攻击必须不易被发现，通常通过限制扰动的幅度，使得新图像在视觉上与原始图像难以区分。上述攻击利用心理黑客技术，将攻击伪装成常见的非对抗行为，如涂鸦或佩戴眼镜。

2. **可打印性**：
   - 攻击的设计==必须考虑到物理实现的可打印性==，需在目标函数中加入可打印性评分（NPS）项。NPS 的定义为：
   $$
   \text{NPS}(x) = \sum_{i} |x_i - b|, \quad b \in B
   $$
   其中，$B$ 是 RGB 颜色域中可打印颜色配置的集合。Sharif 等人建议构建颜色映射来纠正数字与打印颜色之间的差异。

3. **鲁棒性**：
   - 物理实例在实际应用中可能会有对象位置的变化（如轻微旋转）。成==功的攻击必须对这些变化具有鲁棒性==。上述方法通过使用同一目标对象的多幅图像集 $X$，确保单一扰动 $\delta$ 在所有或大多数图像上有效。

### 攻击的数学表述

针对目标攻击，Sharif 等人将优化问题修改为：
$$
\min_{\delta} \sum_{x_0 \in X} l(F(x_0 + \delta), y_T)
$$

## 8.2.5 黑箱攻击

黑箱攻击也适用于深度神经网络，主要思想类似于理论和实践中的决策时间黑箱攻击。查询基础的攻击已被多个研究证明有效。

### 转移性现象

Papernot 等人和 Szegedy 等人观察到的“转移性”现象表明，针对一个学习模型设计的对抗性示例通常对另一个为相同问题训练的模型有效。这种转移性可以在以下情况下观察到：

1. 当模型在不同但相关的数据集上训练时（如第 4 章中的代理数据）。
2. 当代理模型基于对目标模型的查询数据进行学习时。
3. 当使用与生成目标模型不同的算法（代理算法）进行训练和攻击时，例如训练深度神经网络以生成针对逻辑回归模型的对抗示例。
## 8.3 提高深度学习对对抗性示例的鲁棒性

### 背景与动机

深度神经网络的脆弱性引发了对抗性攻击防御的研究。==本节重点介绍如何通过鲁棒优化等方法来增强深度学习模型对对抗性示例的抵抗力==。我们将使用符号 $F(x, \theta)$ 来表示模型预测，以==强调我们在学习参数 $\theta$ 的过程中追求对抗性鲁棒性==。

## 8.3.1 鲁棒优化

==鲁棒学习的核心是对抗风险最小化==，即通过考虑对手可能的攻击来优化模型。假设 $A(x; \theta)$ 是对抗模型，给定输入 $x$ 和参数 $\theta$ 生成对抗示例 $x'$。对抗经验风险最小化的目标函数为：

$$
\sum_{i \in D} l(F(A(x_i; \theta); \theta), y_i)
$$

通过==零和博弈松弛==，==我们可以得到鲁棒优化的形式：==

$$
\min_{\theta} \max_{\|\delta\| \leq \epsilon} \sum_{i \in D} l(F(x_i + \delta; \theta), y_i)
$$

这种框架为构建对抗性鲁棒的深度学习模型提供了理论基础，主要提出了以下三种思路：

1. **对抗正则化**：通过对最坏情况损失的近似，作为正则化项来增强模型的鲁棒性。
2. **鲁棒梯度下降**：利用最坏情况损失函数的梯度来训练模型。
3. **认证鲁棒性**：通过计算最坏情况损失的上界，提供模型的鲁棒性证明。

### 对抗正则化

对抗正则化方法的关键在于==平衡原始数据的准确性与对抗性示例的鲁棒性==。目标函数为：

$$
\min_{\theta} \sum_{i\in D} \left( \alpha \, l(F(x_i; \theta), y_i) + (1 - \alpha) \, l_{wc}(F(x_i; \theta), y_i) \right)
$$

其中，$\alpha$ 是用于平衡的超参数。对抗正则化通常==依赖于简化的攻击模型，如梯度符号攻击==。

### 鲁棒梯度下降

鲁棒梯度下降是一种更精确的方法，==旨在直接计算最坏情况损失的梯度==，以支持模型的训练。Madry 等人提出，==利用鲁棒优化的结果可以近似计算这些梯度，从而在随机梯度下降中使用。==

如果 $\delta^*$ 是最坏情况损失的最优解，则在训练过程中可以使用：

$$
\nabla_{\theta} l(F(x + \delta^*; \theta), y)
$$

作为梯度下降的方向。==这种方法允许使用有效的近似优化器（如 PGD）==来训练鲁棒神经网络。Madry 等人（2018）观察到，==应用鲁棒优化中的重要结果可以近似计算梯度，并在实践中用于梯度下降训练。==具体而言，假设 $\delta^*$ 是最大化最坏情况损失的解，则在随机梯度下降过程中，我们可以用 $\delta^*$ 计算的==损失函数的梯度来进行下一步的梯度下降。==

### 认证鲁棒性

==认证鲁棒性提供了一种理论保障，确保模型在特定对抗性扰动下的鲁棒性。关键在于获得最坏情况损失的可处理上界：==

$$
l_{wc}(F(x; \theta), y) \leq J(x; y; \theta)
$$

Raghunathan 和 Wong & Kolter 的方法首先==对优化问题进行凸松弛==，然后==利用对偶性质计算上界==。通过对 ReLU 激活的线性不等式放松，==最终可以将最坏情况损失的上界转化为线性规划问题==。该方法的优势在于==所得到的上界对参数 $\theta$ 可微==，从而可以与随机梯度下降结合使用，增强模型的鲁棒性。
## 8.3.2 重新训练

重新训练是一种==通用的防御方法==，适用于本章描述的深度学习攻击，==这些攻击被视为决策时间攻击==。具体而言，我们可以通过以下步骤进行迭代训练：

1. **生成对抗示例**：==使用任何攻击模型生成对抗示例==。
2. **扩展训练数据**：将这些对抗示例添加到训练数据中。
3. **重复训练**：==不断重复上述过程。==

这种迭代重新训练方法的一个重要优势是，==它不依赖于构造对抗示例的具体算法。相比之下，基于鲁棒优化的方法通常假设攻击是可靠的，这可能导致解决方案过于保守，无法在实际中有效。==

## 8.3.3 蒸馏 DISTILLATION

蒸馏是一种启发式技术，最初用于将知识从复杂模型转移到较简单模型（主要用于模型压缩）。Papernot 等人（2016）提出利用蒸馏技术增强深度神经网络对对抗噪声的鲁棒性。

蒸馏方法的步骤如下：

1. **数据集准备**：使用原始训练数据集 $D = \{(x_i, y_i)\}$，其中标签 $y_i$ 以独热编码形式表示（即除了真实类别的位为 1 外，其余均为 0）。
   
2. **初始训练**：训练一个深度神经网络==，将 softmax 层中的 softmax 函数替换为==：
   $$
   F_i(x) = \frac{e^{Z_i(x)/T}}{\sum_{j} e^{Z_j(x)/T}}
   $$
   其中 $T$ 是外生选择的温度参数。

3. **新数据集创建**：创建新的训练数据集 $D' = \{(x_i, y_i')\}$，其中 $y_i' = F(x_i)$，$F(\cdot)$ 是之前训练的神经网络返回的软（概率）类别标签。

4. **新网络训练**：在新数据集 $D'$ 上训练新的深度神经网络，使用与第一个网络相同的温度参数 $T$。

5. **最终模型调整**：经过重新训练后，去掉最后一层的温度 $T$（即在测试时设置 $T = 1$，并将 softmax 的输出缩放），从而使预测的类别概率更加尖锐。

尽管防御性蒸馏显示出对多种攻击（如 FGSM 和 JSMA）的有效性，但 Carlini 和 Wagner（2017）证明了 CW 攻击能够有效击败该方法。Carlini 和 Wagner 提出的关键见解是，蒸馏技术在对抗性攻击中有效的原因在于温度参数 $T$ 强化了 $Z_i(x)$ 的值。当 $T$ 设置为 1 时，导致极为尖锐的类别预测，从而使得梯度数值不稳定==。然而，如果攻击者利用最后一层的隐藏层值 $Z(x)$，则梯度又会恢复到良好的行为状态。==