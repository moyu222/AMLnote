---
date: 2024-10-09
aliases:
  - 机器学习入门
tags:
  - AdML
  - uris
  - polyu
---
## 引言

- **机器学习基础**：
  - 机器学习通常分为三大主要领域：
    1. **监督学习**：使用带标签的数据进行训练。
    2. **无监督学习**：使用无标签的数据进行模式识别。
    3. **强化学习**：通过与环境交互学习最佳策略。

- **学习过程的示意图**：
  - 学习被视为一个管道，起始于原始数据（如可执行文件的集合），并附带标签指示文件的良性或恶性。
  
- **数据处理**：
  - 原始数据经过处理后提取数值特征，形成特征向量 \( x_i \)（如指示特定系统调用的二元变量）。
  - 处理后的数据（简称为数据）用于应用学习算法。

- **学习算法输出**：
  - 学习算法的输出是一个模型，可能是数据的数学模型（如其分布），也可能是用于预测未来实例标签的函数。

![[Pasted image 20241009134156.png]]

# 2.1 监督学习

### 处理后的数据与学习算法

- **定义**：
  - 在监督学习中，给定模型类 $F$ 和数据集 $D = \{(x_i, y_i)\}_{i=1}^n$，其中 $x_i \in X \subset \mathbb{R}^m$ 为特征向量，$y_i$ 为标签。

- **数据生成假设**：
  - 数据集通常==假设是从未知分布 $P$ 中独立同分布（i.i.d.）==生成的，即 $(x_i, y_i) \sim P$
### 目标

- **模型寻找**：
  - 目标是找到一个模型 $f \in F$，使得在所有可能的模型中，模型 $f$ 的损失 $l(f(x), y)$ 最小化：
  
  $$
  E_{(x,y) \sim P}[l(f(x), y)] \leq E_{(x,y) \sim P}[l(f'(x), y)] \quad \forall f' \in F
  $$

- **损失函数**：
  - 损失函数 $l(f(x), y)$ 衡量模型 $f$ 在预测真实标签 $y$ 时的误差。

### 实践中的应用

- **==经验风险最小化（ERM）==**：
  - 由于 $P$ 是未知的，使用训练数据 $D$ 来找到一个好的候选模型 $f$，这导致了最小化经验风险的问题：
  
  $$
  \min_{f \in F} \sum_{i \in D} l(f(x_i), y_i) + \lambda(f)
  $$

  - 其中 $\lambda(f)$ ==是正则化项==，旨在惩罚候选模型的复杂度 regularization term
- **参数表示**：
  - 常见的模型类 $F$ 具有参数表示，参数 $w$ 在实向量空间中。ERM 问题通常写作：
  
  $$
  \min_{w} \sum_{i \in D} l(f(x_i | w), y_i) + \lambda(w)
  $$

  - 正则化 $\lambda(w)$ 通常采用 $l_p$ 范数形式，常见的包括 $l_1$ 范数（套索）和 $l_2$ 范数。

### 监督学习的分类

- **回归与分类**：
  - 监督学习通常分为两类：
    1. **回归**：标签为实值，$Y = \mathbb{R}$。
    2. **分类**：标签为有限集合，$Y$ 是有限标签集。

## 2.1.1 回归学习 Regression Learning

在回归学习中，由于==标签是实值==，因此我们不太可能完全正确地预测它们。一个合适的损失函数应该惩罚我们对观察到的标签的偏离程度。通常，这可以通过 $l_p$ 范数来表示：

$$
l(f(x), y) = \|f(x) - y\|_p
$$

实际上，考虑 $l_1$ 范数或 $l_2$（欧几里得）范数通常是最有用的。

### 线性回归示例

为了使回归学习更具体，考虑线性回归作为一个例子。在这种情况下，模型类 $F$ 是所有维度为 $m$ 的线性函数的集合，或等效地，是所有系数 $w \in \mathbb{R}^m$ 和一个偏置项 $b \in \mathbb{R}$ 的集合，其中任意线性函数可以表示为：

$$
f(x) = w^T x + b = \sum_{j=1}^m w_j x_j + b
$$

如果我们引入一个额外的常量特征 $x_{m+1} = 1$，我们可以等效地将线性模型写作：

$$
f(x) = w^T x
$$

因此，我们通常使用后者的形式。我们的目标是找到一些参数化的 $w$ 来最小化训练数据上的误差：

$$
\min_{w \in \mathbb{R}^m} \sum_{i \in D} l(w^T x_i, y_i)
$$

### ==普通最小二乘法（OLS）==

常用的标准普通最小二乘法（OLS）回归允许我们通过将一阶导数设为零来以封闭形式解决这个问题，使用欧几里得（$l_2$）范数作为损失函数。施加 $l_1$（套索）正则化（即将 $\|w\|_1$ 添加到目标中）通常会导致稀疏模型，其中许多特征权重 $w_j = 0$。另一方面，使用 $l_2$ 正则化（$\|w\|_2$）会导致模型系数的大小缩小，但一般不会完全为 0（这被称为岭回归）。
## 2.1.2 分类学习 CLASSIFICATION LEARNING

在分类学习的最基本版本中，我们有两个类。一个方便的编码方式是 $Y = \{-1, +1\}$，这种编码在对抗性环境中特别自然，其中 $-1$ 表示“良性”（正常邮件、合法网络流量等），而 $+1$ 对应于“恶意”（垃圾邮件、恶意软件、入侵尝试）。这种表示方式的便利之处在于，我们可以将分类器表示为：

$$
f(x) = \text{sgn}(g(x))
$$

其中 $g(x)$ 返回一个实值。换句话说，当 $g(x)$ 为负时，我们返回 $-1$ 作为类别，而正的 $g(x)$ 则意味着返回 $+1$。==这种分解对于理解许多分类攻击的性质至关重要。我们将 $g(x)$ 称为分类得分函数，或简单称为得分函数。==

### 理想损失函数

在分类中，“理想”的损失函数通常是指示函数，==当预测标签与实际标签不匹配时为 1，否则为 0==（这通常被称为 0/1 损失）。使用此损失函数的主要挑战在于它==是非凸的 non-convex，这使得经验风险最小化问题变得相当复杂==。因此，通常会使用一些替代方案。

### 基于得分的损失函数 a score-based loss function

一种常见的方法是使用基于得分的损失函数，==该损失函数采用得分函数 $g(x)$ 而不是 $f(x)$ 作为输入==。可以观察到，分类决策是正确的（即 $f(x) = y$）当且仅当：

$$
y g(x) \geq 0
$$
- 省略
	- 也就是说，$y$ 和 $g(x)$ 具有相同的符号。此外，每当它们的符号不同，$|y g(x)|$ 越大，意味着这种差异越大——换句话说，得分函数也允许我们分配误差的大小。因此，基于得分的损失函数自然表示为 $l(y g(x))$。

### 0/1 损失示例

例如，0/1 损失变为：

$$
l_{01}(y g(x)) = 
\begin{cases} 
1 & \text{if } y g(x) \leq 0 \\ 
0 & \text{otherwise} 
\end{cases}
$$

这当然==仍然是非凸的==。典型的方法是使用 0/1 ==损失函数的凸松弛use a convex relaxation of the 0/1 loss function==，结果是对于 $g(x)$ 为模型参数的凸函数，形成一个凸优化问题。常见的例子==有铰链损失hinge loss==：

$$
l_h(y g(x)) = \max\{0, 1 - y g(x)\} \quad (\text{用于支持向量机})
$$

和==逻辑损失logistic loss==：

$$
l_l(y g(x)) = \log(1 + e^{-y g(x)}) \quad (\text{用于逻辑回归})
$$

### 线性分类 

- 以线性分类为例。在这种情况下，$g(x) = w^T x$ 是==一个线性函数，和线性回归一样==，
	- 我们的目标是找到一个最佳特征权重向量 $w$。如果我们使用铰链损失和 $l_2$ 正则化，我们得到以下经验风险最小化（ERM）的优化问题：
	- $$
	\min_{w} \sum_{i \in D} \max\{0, 1 - y_i w^T x_i\} + \|w\|_2^2
	$$
- 这正是==线性支持向量机==所解决的优化问题。

### 多类分类的推广

将这些概念推广到多类分类问题的一种方法是为特征向量 $x$ 和类标签 $y$ 定义一个一般的得分函数 $g(x, y)$。分类决策则为：

$$
f(x) = \arg\max_{y \in Y} g(x, y)
$$

例如，假设 $g(x, y)$ 编码了给定特征向量 $x$ 的标签 $Y$ 的概率分布，即 $g(x, y) = \Pr\{y | x\}$（这在深度神经网络图像分类模型中是常见的）。那么 $f(x)$ 就成为了最可能的标签 $y \in Y$。

## 2.1.3 PAC 学习能力

==**可能近似正确（PAC）学习能力Probably approximately correct (PAC) learnability**是一个重要的理论框架==
### 概念
适用于（大多数监督）机器学习。这里，我们特别针对二分类问题介绍这一概念。正式地，设 $F$ 为防御者考虑的可能分类器的类（即，防御者的假设或模型类）。设 $(x, y) \sim P$ 为实例，其中 $x \in X$ 是输入特征向量，$y$ 是标签，取值为 $\{0, 1\}$。为了简化表述，假设 $y = h(x)$，其中 $h(x)$ 是不一定在 $F$ 中的某个函数（即，输出是输入 $x$ 的确定性函数，例如，$x$ 的真实分类为良性或恶意）。

对于任意 $f \in F$，定义 $e(f) = \Pr_{x \sim P}[f(x) \neq h(x)]$ 为相对于 $P$ 的期望误差，并且定义 $e_F = \inf_{f \in F} e(f)$ 为任何函数 $f \in F$ 可以达到的最优（最小）误差。设 $z_m = \{(x_1, y_1), \ldots, (x_m, y_m)\}$ 为根据 $P$ 生成的数据，$Z_m$ 为所有可能的 $z_m$ 的集合。

#### 定义

**定义 2.1** 设 $F$ 为一个将 $x$ 映射到 $\{0, 1\}$ 的函数类。学习算法是一个函数 $L: [m-1] Z_m \to F$。我们说 $F$ 是 PAC 可学习的，如果存在一个学习算法，对于任意的 $\epsilon, \delta \in (0, 1)$ 和任意的 $P$，存在 $m_0(\epsilon, \delta)$，使得对于所有 $m \geq m_0(\epsilon, \delta)$，有：


$$
\Pr_{z_m \sim P}[e(L(z_m)) \leq e_F + \epsilon] \geq 1 - \delta
$$

我们称如果 $m_0(\epsilon, \delta)$ 为多项式在 $1/\epsilon$ 和 $1/\delta$ 中，并且存在一个学习算法在时间上是多项式的，具有样本复杂度 $m_0(\epsilon, \delta)$，则我们称其为有效（多项式）PAC 可学习的。

因此，今后我们通常省略 PAC 修饰符，仅用“可学习”一词表示 PAC 可学习。我们称能够获得 PAC 保证的算法为 PAC 学习算法；如果该算法在多项式时间内运行，并且具有多项式样本复杂度，我们称其为多项式 PAC 学习算法。

### 理解
PAC（可能近似正确）学习是机器学习中的一个重要理论框架，主要用于描述学习算法的有效性和可靠性。通俗地说，PAC 学习主要关注以下几个方面：

1. **目标**：
   - 学习算法的目标是从有限的训练数据中学习一个模型，使得这个模型在面对新数据时也能做出准确的预测。

2. **近似正确**：
   - “可能近似正确”意味着算法可能不会总是找到完美的模型，但它能以高概率找到一个在特定误差范围内的好模型。例如，如果模型的预测错误率低于某个阈值（比如 5%），那么这个模型就被认为是“近似正确”的。

3. **样本复杂度**：
   - PAC 学习还关注需要多少训练样本才能保证学习得到一个好的模型。样本复杂度是指为了达到所需的准确度，算法需要多少数据。如果样本量足够大，算法就有可能学习到一个“好”模型。

4. **概率保证**：
   - PAC 学习提供了一种概率保证，即给定足够的训练数据，算法将以很高的概率（例如 95%）找到一个在误差范围内的模型。

5. **多项式时间**：
   - 理想的学习算法应该在多项式时间内运行，即算法的运行时间与输入数据的规模之间呈多项式关系。这确保了算法在实际应用中是可行的。
### 总结

简单来说，PAC 学习是一个理论框架，它帮助我们理解如何从有限的数据中学习一个模型，确保这个模型在面对新数据时表现良好，并且给出在什么条件下我们可以信赖这个模型的结果。通过这个框架，研究人员能够评估不同学习算法的有效性和效率。

## 2.1.4 对抗环境中的监督学习

### 回归学习在对抗环境中的应用

在对抗环境中使用回归学习的一个例子是从实际控制决策的观察中学习的参数控制器，这在自动驾驶的背景下得到了验证。简而言之，假设我们学习一个控制器 $f(x)$ 来预测转向角度，该角度作为基于视觉的输入的函数（图 2.2 左侧），这些输入被捕获为特征向量 $x$，就像在端到端自动驾驶中所做的那样 [Bojarski et al., 2016; Chen and Huang, 2017]。==对手可能会对视觉系统捕获的图像进行小的操控，从而将 $x$ 修改为 $x'$，以引入在预测转向角度 $f(x')$ 中的误差，从而最大化与真实最优角度 $y$ 的差异（图 2.2 右侧）。==

### 示例

- **攻击示例**：
  ![[Pasted image 20241009145410.png]]

- **股票价格预测**：
  - 另一个例子是学习者可能希望预测股票价格 $f(x)$，作为观察值 $x$ 的函数。对手希望通过影响观察状态 $x$ 来从学习者的错误中获利，他们可能会尝试将其操控为另一个 $x'$，以便在下一期预测价格过高。这可能导致学习者愿意以高价从对手那里购买股票，从而为对手创造有效的套利机会，而学习者则受到损害。

### 分类学习在对抗环境中的应用


在对抗环境中应用二分类通常意味着区分良性实例和恶意实例。例如，在电子邮件过滤中，恶意实例可能是垃圾邮件或钓鱼邮件，而良性实例则是正常的电子邮件流 [Bhowmick and Hazarika, 2018]。在恶意软件检测中，恶意实体自然指的是恶意软件，而良性实例则对应于非恶意可执行文件 [Chau et al., 2011; Smutz and Stavrou, 2012; Šrndić and Laskov, 2016; Tamersoy et al., 2014; Ye et al., 2017]。在信用卡欺诈中，特定信用卡申请的特征被考虑，以确定申请是欺诈性的（恶意）还是合法的（良性） [Lebichot et al., 2016; Melo-Acosta et al., 2017]。

在所有这些情况下，对手都有避免被检测的动机，他们希望操控其行为，使其在检测器看来是良性的。

# 2.2 无监督学习

在无监督学习中，数据集仅由特征向量组成，而没有标签：$D = \{x_i\}$。因此，无监督学习中的问题主要关注观察特征的联合分布的特征，而不是预测目标标签。然而，监督与无监督技术之间的界限有时可能模糊，例==如在矩阵补全方法中，其目标是预测未观察到的矩阵条目。==

无监督学习下有许多常见的具体问题，我们讨论其中的三个：==聚类、主成分分析和矩阵补全。==

## 2.2.1 聚类

无监督学习中最常见的例子之一是聚类任务，在该任务中，数据集中的特征向量被划分为一组子集 $S$，使得每个集合 $S \in S$ 中的特征向量在某种接近度的度量下“接近”集合 $S$ 的均值特征向量。

形式上，聚类可以看作是解决以下优化问题：

$$
\min_{S, \mu_S} \sum_{S \in S} \sum_{i \in S} l(x_i, \mu_S
) 
$$

其中 $S$ 是 $D$ 的一个划分，$\mu_S$ 是聚类 $S \in S$ 中数据的聚合度量，例如其均值。该问题的一个常见版本==使用 $l_2$ 范数作为损失函数==，相关问题的启发式近似是 $k$-均值聚类，其中通过迭代更新聚类均值并将数据点移动到具有最近均值的聚类来进行。

此外，还可以添加正则化以控制模型复杂度（例如，如果没有事先指定，则形成的聚类数量）。

## 2.2.2 主成分分析

==主成分分析（PCA）Principal component analysis (PCA) ==
### 公式
寻找一组 $K < m$ 的正交基向量 $\{v_k\}_{k=1}^K$，这些向量是数据矩阵 $X$ 的特征向量，其中数据集 $D$ 中的每个特征向量 $x_i$ 是该矩阵的一行。等价地，每个 $v_k$ 是通过以下方法求解的：


$$
v_{k-1} = \arg\max_{v_k \perp v_j, j < k} \left\{ \text{tr}(X(I - X v_i v_i^T)v_k) \right\}
$$

设 $V$ 为 PCA 生成的基矩阵，其中列对应于特征向量 $v_k$。对于任何特征向量 $x$，其 $m$ 维重构为：

$$
\hat{x} = V V^T x
$$

相应的残差误差（即使用 PCA 近似原始特征向量 $x$ 产生的误差）为：

$$
e_x = x - \hat{x} = (I - V V^T)x
$$

直观上，如果原始数据可以有效地表示在 $K$ 维子空间中（其中 $K$ 相对原始维度 $m$ 较小），我们期望 PCA 是有效的；换句话说，残差误差的大小 $||e_x||$ 应该较小。

### 简介
主成分分析（PCA）是一种常用的数据降维技术，主要用于简化数据集，同时尽可能保留其重要信息。通俗地说，PCA 的主要目标和步骤如下：

### 主要目标

- **降维**：通过减少特征的数量，使数据更易于处理和可视化，同时尽量保留原始数据的信息。
- **去相关性**：将数据转换为一组新的变量，这些变量相互之间不相关，称为主成分。

### 工作原理

1. **标准化数据**：
   - 首先，对数据进行标准化处理，使得每个特征的均值为 0，方差为 1。这有助于消除特征之间的尺度差异。

2. **计算协方差矩阵**：
   - 对标准化后的数据计算协方差矩阵，以了解不同特征之间的关系。

3. **特征值分解**：
   - 对协方差矩阵进行特征值分解，找到特征值和对应的特征向量。特征值表示每个主成分的方差，特征向量则表示主成分的方向。

4. **选择主成分**：
   - 根据特征值的大小选择前 K 个特征向量（主成分），通常选择方差较大的主成分，以保留更多的信息。

5. **转换数据**：
   - 使用选定的主成分将原始数据投影到新空间中，从而得到降维后的数据。

### 应用场景

- 数据可视化：在 2D 或 3D 空间中可视化高维数据。
- 噪声过滤：通过保留主要成分来降低数据噪声。
- 特征选择：减少计算复杂度，提高模型性能。

## 2.2.3 矩阵补全 MATRIX COMPLETION

矩阵补全问题的一个动机可以通过 Netflix 的协同过滤问题来说明。目标是预测给定用户对某部电影的喜好程度（或更准确地说，是评分）。可以想象一个大的矩阵 $M$，其中有 $n$ 行对应用户，$m$ 列对应电影，每个矩阵条目表示用户 $i$ 对电影 $j$ 的评分。==因此，我们的预测问题可以等价地视为预测这个大矩阵中第 $ij$ 个条目的值，而这个矩阵由于用户提供的实际电影评分而非常稀疏。==

### 关键见解

关键的见解是==假设真实的评分矩阵是低秩的==；例如，相似的用户往往会对相似的电影给出相似的评分。因此，我们可以将真实矩阵分解为 $M = UV^T$，其中 $U$ 和 $V$ 具有 $K$ 列，而 $K$ 是 $M$ 的秩。我们的目标是获取 $U$ 和 $V$，使得 $U_i V_j^T$ 是观察到的条目 $M_{ij}$ 的良好近似。

### 矩阵补全的形式化

令 $M \in \mathbb{R}^{n \times m}$ 为数据矩阵，其中 $M_{ij}$ 表示第 $i$ 个用户对第 $j$ 个项目的评分。我们使用 $\Omega = \{(i,j) \mid M_{ij} \text{ 是观察到的}\}$ 来表示所有观察到的条目，并假设 $|\Omega| \ll mn$。矩阵补全的目标是从少量观察中恢复完整的矩阵 $M$ [Candès和Recht, 2007]。

### 问题挑战

矩阵补全问题通常是病态的，因为不可能用部分观察来补全任意矩阵。因此，需要对底层数据矩阵 $M$ 施加额外的假设。一个标准假设是 $M$ 非常接近一个秩为 $K$ 的矩阵，其中 $K \leq \min(n, m)$。

#### 具体
在这种假设下，可以通过解决以下优化问题来恢复完整矩阵 $M$：

$$
\min_{X} \|R_\Omega(M - X)\|_F^2 \quad \text{s.t.} \quad \text{rank}(X) \leq K
$$

其中 $\|\cdot\|_F$ 表示 Frobenius 范数，$R_\Omega(A)_{ij} = A_{ij}$ 如果 $(i,j) \in \Omega$，否则为 0。不幸的是，式（2.5）中的可行集是非凸的，使得优化问题难以解决。

### 解决方法

为了解决这个问题，文献中提出了两个标准方法：交替最小化和核范数最小化。
#### 具体
1. **交替最小化**：
   通过以下问题进行优化：

   $$
   \min_{U, V} \|R_\Omega(M - UV^T)\|_F^2 + \lambda_U \|U\|_F^2 + \lambda_V \|V\|_F^2
   $$

   这里，$\lambda_U$ 和 $\lambda_V$ 是正则化参数，通常设置为小的正数以避免完成矩阵中的大条目，并改善收敛性。

2. **核范数最小化**：
   通过解决以下核范数最小化问题：

   $$
   \min_{X} \|R_\Omega(M - X)\|_F^2 + \lambda \|X\|_*
   $$

   其中 $\|X\|_*$ 是矩阵 $X$ 的核范数，作为秩函数的凸代理。此问题是一个凸优化问题，可以使用迭代奇异值阈值算法来解决。

这两种方法在特定条件下能够有效地近似真实的底层数据矩阵 $M$。


## 2.2.4 对抗环境中的无监督学习

在对抗环境中，无监督学习的两个常见用途是==攻击聚类和异常检测。==

### 聚类

攻击的聚类在恶意软件取证分析中非常有用，不同的恶意软件变体可以被聚类，

### 异常检测

在异常检测中，通常使用一组“正常”的操作数据来开发“正常”系统行为的模型。例如，可以收集组织中的常规网络流量的痕迹，并识别这些“正常”流量的统计特征。最终目标是识别异常行为，暗示可能是系统故障（在非对抗环境中）或攻击（如入侵）。

#### 中心异常检测

一种简单而通用的异常检测方法是中心异常检测 [Kloft和Laskov, 2012]。在这种方法中，使用训练数据 $D$ 计算均值 $\mu_D = \frac{1}{n} \sum_{i \in D} x_i$，如果新的特征向量 $x$ 的距离满足：

$$
\|x - \mu\|_p \geq r
$$

则标记为异常。

#### PCA 基础的异常检测

另一种重要的异常检测方法利用观察到的正常行为（如网络流量）通常具有较低的内在维度的事实 [Lakhina et al., 2004]。使用 PCA 识别数据的高质量低维表示，并使用残差的大小来检测异常。

#### n-gram 方法

第三种变体使用 n-grams（对象中 n 个连续实体的序列）来确定其是否类似于正常对象的统计模型。在基于网络入侵检测的 n-grams 方法中，首先收集网络数据，开发正常的统计模型。

# 2.3 强化学习

我们首先介==绍马尔可夫决策过程（MDPs），它为强化学习（RL）提供了数学基础 ==[Sutton 和 Barto, 1998]。

### 马尔可夫决策过程（MDP）

一个离散时间折扣无限时间范围的 MDP（为了简化表述，我们将重点关注这一类）由五元组 $\langle s, a, T, r, \gamma \rangle$ 描述，其中：
- $s$ 是状态集合（我们这里假设是有限的）。
- $a$ 是动作集合（同样假设是有限的）。
- $T$ 是转移动态，表示在状态 $s$ 下采取动作 $a$ 后转移到下一个状态 $s'$ 的概率，通常表示为 $T(s' | s, a)$。
- $r(s, a)$ 是期望奖励函数。
- $\gamma \in [0, 1]$ 是折扣因子。

在 MDP 和 RL 中的两个核心概念是值函数和 Q 函数：
- 值函数定义为：
  $$
  V(s) = \max_a \left[ r(s, a) + \gamma \sum_{s'} T(s' | s, a) V(s') \right]
  $$
- Q 函数定义为：
  $$
  Q(s, a) = r(s, a) + \gamma \sum_{s'} T(s' | s, a) V(s')
  $$

### 概念理解

==值函数捕捉了在遵循最优策略时可以获得的最优折扣奖励总和，而 Q 函数则表示在状态 $s$ 下采取动作 $a$ 后，继续遵循最优策略所获得的折扣奖励。==注意到 $V(s) = \max_a Q(s, a)$。
#### 求解
对于 MDP，有多种已知的方法来计算最优策略，其中一个例子是值迭代。在值迭代中，可以将值函数的描述转化为一个迭代过程，计算第 $i+1$ 次迭代中的值函数 $V_{i+1}(s)$：

$$
V_{i+1}(s) = \max_a \left[ r(s, a) + \gamma \sum_{s'} T(s' | s, a) V_i(s') \right]
$$

我们可以用任意值函数初始化这个过程，并且它在极限中总会收敛到真实的值函数。

### 策略迭代

另一种替代方法==是使用策略迭代==，其中交替进行策略评估（计算当前策略的值）和改进步骤（类似于值迭代步骤）。最后，可以通过线性编程计算最优值函数。通过简单地在每个状态中找到最大化的动作，可以从值函数中提取最优策略。

### 强化学习中的学习过程

在强化学习中，已知 $s, a, \gamma$，但不知道 $T$ 或 $r$。然而，我们仍然可以通过经验学习，最终获得接近最优的策略。有多种算法用于此目的，==其中最著名的可能是 Q 学习。==

在 Q 学习中，我们以任意方式初始化 Q 函数。在任意迭代 $i + 1$ 中，观察状态 $s_i$ 并根据当前策略采取动作 $a_i$，该策略确保任何动作在任何状态下都有正概率被采取。如果我们观察到奖励 $r_{i+1}$ 和下一个状态 $s_{i+1}$，可以按如下方式更新 Q 函数：

$$
Q_{i+1}(s_i, a_i) = Q_i(s_i, a_i) + \alpha_{i+1} \left( r_{i+1} + \gamma \max_a Q_i(s_{i+1}, a) - Q_i(s_i, a_i) \right)
$$

### 探索与利用

一种简单的计算基于 Q 函数的策略的方法是简单地在每个状态中选择当前迭代 $i$ 中最大化 $Q_i(s, a)$ 的动作。然而，这将意味着没有进行探索。一个简单的修改是使用 $\epsilon$-==贪婪策略==，以概率 $1 - \epsilon_i$ 采取这样的策略，而以概率 $\epsilon_i$ 采取随机动作。可以随着时间的推移减小 $\epsilon_i$。

### 结构化领域中的应用

当 RL 应用于结构化领域（许多实际应用的特征）时，状态通常由一组变量 $x = \{x_1, \ldots, x_n\}$ 组成（这通常被称为状态的分解表示）。在这种情况下，策略无法合理地表示为查找表。可以通过学习 Q 函数的参数化表示 $Q(x, a | w)$，并利用它间接表示策略，其中 $\pi(x) = \arg\max_a Q(x, a | w)$。

## 2.3.1 在对抗环境中的强化学习

在潜在的对抗环境中，==强化学习的一个应用示例是自主控制==（例如，自动驾驶汽车）在存在可能影响观察状态的对手的情况下。==由于最优策略依赖于状态，因此状态的修改可能导致错误的决策==。

具体的攻击模型通常利用分解状态表示：攻击者通常只能修改状态变量的一个（小）子集，从而使学习到的策略偏离。实际上，这种攻击并不特定于 RL，而是攻击特定的策略（如果攻击者已知该策略）。攻击也可以在学习过程中进行，导致学习到糟糕的策略——例如，通过影响观察到的奖励和观察到的状态。在任何情况下，攻击的结果将导致基于 RL 的自主代理犯错误，可能导致高风险的情况，例如导致自动驾驶汽车发生碰撞。