---
date: 2024-10-14
aliases:
  - 防御决策时间攻击
tags:
  - AdML
  - uris
  - polyu
---
## 引言

在本章中，我们将探讨如何防御这些攻击。
本章结构如下：

1. **二元分类器的最优加固**：讨论如何使==二元分类器==在决策时间（主要是规避）攻击下具有鲁棒性，并关注几个可以最优解决的重要特例。
2. **近似最优分类器加固**：介绍可扩展性显著更高且在某些情况下更通用的方法，但仅是近似最优。
3. **决策随机化**：描述如何==通过原则性随机化principled randomization.==加固一般类型的二元分类器。
4. **线性回归的加固**：简要描述一种加固线性回归以应对决策时间攻击的方法。

## 5.1 强化监督学习，防止决策时间攻击

这一部分深入探讨了如何增强监督学习模型，以有效抵御决策时间攻击，尤其是在对抗性数据操控的背景下。
### 主要内容：

1. **学习目标**：
   - 在没有对抗性干扰的情况下，监督学习的目标是学习一个函数 $f$，使得：
     $$
     E_{(x,y) \sim P}[l(f(x), y)] \leq E_{(x,y) \sim P}[l(f'(x), y)] \quad \forall f' \in F
     $$
     这里，$P$ 是数据的未知分布，$l$ 是损失函数。期望风险 $R(f)$ 是模型性能的量化标准。

2. **对抗性监督学习**：
   - 在对抗性学习中，假设未来的数据分布与训练数据有所不同。攻击者可以通过对抗目标集 $S \subseteq X \times Y$ 中的实例，修改特征向量以导致分类错误。攻击的目标可能是：
     - **定向攻击**：将某些样本伪装成另一类。
     - **最大化错误**：让模型尽可能多地出错。

3. **决策时间攻击的表示**：
   - 把决策时间攻击抽象为函数 $A(x \mid f)$，该函数接受原始特征向量 $x$ 和学习模型 $f$，返回修改后的特征向量。目标是：
     - 修改特征向量，使预测标签 $f$ 发生变化。

4. **对抗经验风险**：
   - 定义期望对抗经验风险 $R_A(f)$，将其分为两部分：
     - 针对对抗实例（通过函数 $A$ 处理）。
     - 针对未修改的非对抗实例。
   - 公式表示为：
     $$
     R_A(f) = E_{(x,y) \sim P}[l(f(A(x \mid f)), y) \mid (x,y) \in S] + E_{(x,y) \sim P}[l(f(x), y) \mid (x,y) \notin S]
     $$

5. **优化问题**：
   - 与传统学习类似，优化问题为：
     $$
     \min R_A(f)
     $$
   - 由于无法直接知道分布 $P$，使用训练数据集 $D = \{(x_i, y_i)\}_{i=1}^n$ 作为代理，以近似优化。

6. **Stackelberg 博弈模型**：
   - 将学习过程视为 Stackelberg 博弈，其中：
     - **领导者**：学习者选择模型 $f$。
     - **跟随者**：攻击者观察 $f$ 并选择最优攻击策略 $A(x_i \mid f)$。
   - 目标是找到 Stackelberg 均衡，使得：
     1. 攻击者的响应 $A(x_i \mid f)$ 是针对给定 $f$ 的最佳选择。
     2. 学习者的模型 $f$ 最小化对抗经验风险。
	
	![[Pasted image 20241014210454.png]]

7. **示例**：
   - 通过一个具体的例子说明如何选择阈值以区分良性和恶意样本。考虑特征空间 $x \in [0, 1]$，数据集 $D = \{(0.25, \text{良性}), (0.75, \text{恶意})\}$。此外，攻击者的目标是最小化将 $x$ 修改为良性所需的变化，同时约束总变化最多为 0.5（对应于公式 (4.7) 及附加预算约束 $C = 0.5$）。目标是找到阈值 $r$：
     - 当 $x > r$ 被视为恶意，$x \leq r$ 被视为良性。
     - 选择阈值 $r = 0.5$ 是一个自然的基线，但攻击者可以轻松将实例修改为低于 0.5，因此此阈值并不鲁棒。
     - 选择阈值 $r = 0.25$ 则可确保鲁棒性，因为在攻击者的修改限制下，无法成功逃避分类器。

8. **平局处理** tie-breaking：
   - 在 Stackelberg 博弈中，使用强 Stackelberg 均衡Strong Stackelberg equilibrium（SSE）来处理攻击者的最佳响应平局，通常在学习者的有利方向上进行打破。这确保了学习者能够做出小幅调整，以提高对抗性的效果。

9. **假设与实际应用**：
   - 尽管攻击者可能不知道分类器 $f$ 的具体形式，但在鲁棒学习的背景下，合理假设攻击是白箱的，即学习者知道模型 $f$。
   - 实际上，博弈是在未知攻击者分布的情况下进行的。训练数据集可以作为这种分布的代理，符合学习的标准做法。

## 5.2 最优规避鲁棒分类

这一部分讨论了==针对决策时间攻击的学习强化方法==，专注于==最优规避鲁棒的二分类==问题，目标是最小化对抗性经验风险（AER）。数据集被分为两部分：$D_C$（恶意实例）和 $D_0$（良性实例）。

### 5.2.1 最优规避鲁棒稀疏 SVM  OPTIMAL EVASION-ROBUST SPARSE SVM

我们介绍了一种针对==线性支持向量机==（SVM）的最优规避鲁棒二分类算法，适用于第4章描述的大多数规避攻击模型。该算法由 Li 和 Vorobeychik 提出，==使用 $l_1$ 正则化。==

#### 优化问题的重写
- 针对 $l_1$ 正则化 SVM 的对抗性经验风险最小化问题被重写为一个双层程序：
  
$$
\min \sum_{i \in D_0} l(1, w^Tx) + \sum_{i \in D_C} l(1, w^T x_e) + \lambda ||w||_1
$$

其中 $l_H(y, w^Tx) = \max(0, 1 - yw^Tx)$ 是铰链损失，$l_A(x, x_i | w)$ 是攻击者希望最小化的对抗损失函数。

#### 约束条件
- 攻击者的决策依赖于其预算约束，确保恶意实例被分类为良性。例如，预算约束定义为 $C_i = \{z | c(z, x_i) \leq C\}$。

#### 数学程序的构建
- 为了求解上述优化问题，将其表达为大型的数学程序，并通过线性化铰链损失和 $||w||_1$ 来简化。

#### 求解过程
- 设计辅助矩阵 $T$ 和二进制矩阵 $Q$，以表示攻击策略和损失。通过引入变量 $z_{ai}$ 来选择特定的攻击特征向量。

#### 最终数学规划
- 完整的数学程序包含多个约束，其中包括通过引入新变量 $m_{ai}$ 来线性化约束。

### 5.2.2 针对自由范围攻击的规避鲁棒 SVM  SVM AGAINST FREE-RANGE ATTACKS

本节讨论了==针对自由范围攻击==的规避鲁棒学习。此类攻击的一个重要特征是==攻击者的目标是最大化训练数据中对抗实例的损失==，这作为对抗风险的代理，受一系列线性约束的限制。我们将线性 SVM 优化问题扩展为：

$$
f(x) = w^T x + b
$$

在此公式中，权重向量 $w$ 和偏置项 $b$ 都是显式的，因为对应的常数特征不会受到攻击。
#### 对抗铰链损失

定义对抗铰链损失如下：

$$
h(w, b, x_i) = 
\begin{cases} 
\max(0, 1 - (w^T x_Q^i + b)) & \text{if } y_i = 1 \\
\max(0, 1 + (w^T x_i + b)) & \text{if } y_i = -1 
\end{cases}
$$

受约束条件 $C_f^{min} \leq x_Q^i \leq C_f^{max}$。

#### 最优化问题

在标准 SVM 风险公式的基础上，得到以下最优化问题：

$$
\min \sum_{i \in D_0} \max(0, 1 + (w^T x_i + b)) + \sum_{i \in D_C} \max(0, 1 - (w^T x_Q^i + b)) + \lambda ||w||^2
$$

结合正负实例的情况，这等价于：

$$
\min \sum_{i} \max(0, 1 - y_i (w^T x_i + b)) + \lambda ||w||^2
$$

#### 线性规划

通过选择合适的 $x_Q^i$，我们可以将最坏情况下的铰链损失最小化，形成以下线性规划：

$$
\arg\min_{w, b, t_i, u_i, v_i} \sum_{i} t_i + \lambda ||w||^2
$$

#### 对偶线性规划

接下来，通过求解该线性规划的对偶形式，可以将对抗版本的 SVM 优化问题写为：

$$
\min \sum_{j} v_j (C_f^{max} - x_i) - u_j (C_f^{min} - x_i)
$$

#### 最终二次规划

通过添加松弛变量和线性约束，我们可以将问题重写为以下二次规划：

$$
\min \sum_{i} \eta_i + \lambda ||w||^2
$$




### 5.2.3 针对受限攻击的规避鲁棒 SVM  RESTRAINED ATTACKS

在受限攻击模型中，==我们修改铰链损失模型并按照相同的步骤解决问题==。
#### 具体
新的铰链损失模型为：

$$
h(w, b, x_i) = 
\begin{cases} 
\max(0, 1 - (w^T x_Q^i + b)) & \text{if } y_i = 1 \\
\max(0, 1 + (w^T x_i + b)) & \text{if } y_i = -1 
\end{cases}
$$

对于最坏情况下的铰链损失，最小化问题可以表示为：

$$
\min \frac{1}{2} (1 + y_i) w^T \xi_i
$$

通过引入线性约束和松弛变量，将该问题重新写为：

$$
\min \sum_{i} t_i + \lambda ||w||^2
$$

### 5.2.4 针对无限制特征空间的规避鲁棒分类 UNRESTRICTED FEATURE SPACES

==make an assumption that the feature space is unrestricted, i.e., X = R^m==
#### 面临的优化问题
在无限制特征空间中，假设攻击者面临的优化问题为：

$$
\max \sum_{i} l_A(w^T(x_i + z_i)) + \beta \sum_{i} Q(z_i)
$$

这里 $l_A(·)$ 和 $Q(·)$ 是凸函数，并且 $z$ 没有约束。假设 $Q(z_i) = ||z_i||^2$，则优化问题表征了学习者和攻击者的 Stackelberg 均衡决策。

重要的限制条件包括假设攻击者可以任意修改特征，这在实际中不太可能，而特征通常有上限和下限。另外，假设损失函数严格凸且连续可微，这排除了常见的损失函数如铰链损失和稀疏（$l_1$）正则化。

### 5.2.5 对抗性缺失特征的鲁棒性

在对抗性攻击中，一==个重要的特例是攻击者在预测时选择性地将某些特征设为零，或有效地将其移除==。这里我们探讨这种攻击及其防御措施，==特别是在支持向量机（SVM）==的背景下。
#### 攻击概述

对于给定的数据点 $(x, y)$，攻击的目标是最大化铰链损失，同时限制最多只能移除 $K$ 个特征。==攻击者的策略是删除对模型决策影响最大的特征，即那些与预测结果 $y w_j x_j$ 相关性最高的特征。==因此，攻击者的最优选择是移除这些特征，以最大化模型的误分类风险。

#### 最坏情况铰链损失

在这种情况下，最坏情况下的铰链损失可以通过考虑移除特征后的模型输出进行计算。攻击者需要找到一个特征移除的组合，使得模型的输出最差，从而导致最大的损失。

#### 防御策略

为了解决这个问题，我们可以==将攻击者的优化问题转化为一个线性规划或二次规划==。通过对铰链损失的最坏情况进行建模，可以将其纳入 SVM 的标准优化框架中。具体来说，我们可以==用最坏情况的铰链损失替换掉原有的铰链损失，从而使得整体优化问题变为一个凸的二次规划。==

#### 关键概念总结

- **对抗性缺失特征**：攻击者选择性地移除某些特征以增加模型的误分类风险。
- **最坏情况铰链损失**：通过考虑最可能被移除的特征，来计算模型在这些特征缺失情况下的损失。
- **优化方法**：将攻击者的最优化问题转化为线性或二次规划，以便有效求解并增强模型的鲁棒性。

这种方法提供了一种防御机制，使得模型在面对对抗性缺失特征时，仍能保持一定的分类性能。

## 5.3 大致增强分类器以抵御决策时间攻击 APPROXIMATELY HARDENING CLASSIFIERS AGAINST DECISION-TIME ATTACKS

即使在针对特定 SVM 损失函数和 $l_1$ 正则化下寻找最佳规避鲁棒线性分类器的过程中，这个问题依然非常复杂。即使是那些较为可处理的优化方法，也通常对攻击者或学习损失函数做出强假设==，因此在可扩展性上存在限制。==

许多先前的研究尝试通过原则性近似来解决直接优化对抗性经验风险的可扩展性问题，这些技术主要可以分为三类：

1. **对抗风险函数的放松**：将复杂的对抗性问题转化为更简单的形式，使得优化过程更加高效。
   
2. **特征空间的放松**：将特征视为连续变量，从而简化计算和优化过程。

3. **使用对抗数据的迭代再训练**：通过不断引入对抗性样本进行再训练，增强模型的鲁棒性。

### 5.3.1 放松方法

放松对抗风险函数的一种常见方法是将问题转化为零和对抗的形式。在这种形式下，攻击者与分类器之间形成一种博弈关系。攻击者的目标是尽可能地“破坏”分类器的决策，而分类器则试图最大化其准确性。

在这种情况下，通常会施加一些约束，比如对特征的修改范围进行限制，以此来模拟现实中攻击者的能力。例如，限制特征的变化在某个固定的范围内（如 $l_p$ 范数）。

这种放松方法的一个重要结果是，它与正则化之间建立了联系。具体来说，增强对抗鲁棒性往往意味着对权重的 $l_1$ 范数施加惩罚。这种联系表明，通过适当的正则化手段，可以提升模型抵御对抗性攻击的能力。

#### 关键概念

- **对抗风险放松**：将复杂的对抗性问题简化为可处理的优化形式。
- **特征空间的连续性**：通过将特征视为连续变量，简化优化过程。
- **迭代再训练**：通过不断引入对抗性样本增强模型的鲁棒性。
- **正则化与鲁棒性**：通过对权重施加惩罚，提升模型对攻击的抵御能力。

这些方法提供了有效的策略，以提高分类器在面对对抗性攻击时的稳定性和准确性。

### 5.3.2 通用防御：迭代再训练

==迭代再训练是一种使监督学习对抗对抗性规避的通用方法==，这种方法已经存在多年。迭代再训练的基本思想是：==首先使用标准学习算法对训练数据进行训练，然后将每个对抗特征向量转换为新的特征向量，并将其添加到数据集中。这个过程会反复进行，直到达到收敛或者达到固定的迭代次数。==

#### 迭代再训练算法

1. **输入**：训练数据 $D$ 和对抗攻击函数 $A(x_i | f)$。
2. **训练**：使用当前数据集训练模型。
3. **转换**：对训练数据中的每个样本==进行对抗特征转换，生成新的特征向量。==
4. **添加**：将新的特征向量添加到数据集中。
5. **重复**：继续进行训练和转换，直到满足终止条件。

#### 关键点

- **终止条件**：一个自然的==终止条件是当前迭代中没有新对抗实例被添加到数据集中。==这意味着算法可能已收敛，并且此时的模型的经验风险是最优对抗经验风险的上界。
  
- **理论性质**：如果算法达到终止条件，结果将具有良好的理论性质，即==模型的经验风险是对抗风险的上界。==

- **适用性**：迭代再训练方法不对攻击者的行为或学习算法做出假设，==因此适用于多分类和回归设置。==

- **收敛性**：==虽然该算法不一定总是收敛==，或者可能需要很大的迭代次数，但许多实证研究表明，这一简单的想法在实践中是有效的。

## 5.4 通过特征级保护增强规避鲁棒性 THROUGH FEATURE-LEVEL PROTECTION

另一种增强对抗鲁棒性的方法是==选择一组“受保护”的特征==，这些特征通过冗余或验证等手段，使攻击者无法修改。这种方法在某些情况下非常有效，尤其==当特征对应于传感器测量，且对抗攻击涉及对这些测量进行修改时。==

#### 特征选择与保护

在二分类情况下，可以将这个问题形式化为：

- **目标**：最小化损失函数，同时约束攻击者的修改范围。
- **可行集**：==针对选择的受保护特征，定义可行的攻击集==。

#### 关键概念

- **受保
- 护特征**：选择特定特征，通过某种方式保护这些特征，使得攻击者无法轻易修改。
- **约束优化**：通过约束攻击者的修改范围，增强模型的鲁棒性。
- **近似解法**：可以通过计算初始权重并贪婪选择特征来获得近似解。

这种方法为增强模型抵抗对抗性攻击提供了新的思路，尤其在面对特定类型的攻击时具有重要意义。

## 5.5 决策随机化. DECISION RANDOMIZATION

在增强机器学习对决策时间攻击鲁棒性的方法中，==大多数技术涉及对训练过程的修改，以获得更为稳健的固定模型==。然而，通过 Stackelberg 博弈的视角，我们可以引入一种强有力的工具——==随机化，这种方法在物理安全应用==中得到了广泛的应用。

### 5.5.1 模型

在 Stackelberg 博弈中，防御者==首先选择标记某个实例为恶意的概率==，随后攻击者基于这一决策选择最优的规避策略。防御者的决策由函数  $q(x)$ 表示，表示对特征向量  $x$  的“行动”概率（例如，标记为恶意）。

该方法的核心思想是==将预测问题与基于预测的决策问题分开。==首先，应用传统的机器学习方法获得一个概率模型 \( p(x) \)，表示在当前恶意和良性行为的分布下，特征向量 \( x \) 是对抗“理想”特征向量的概率。接下来，防御者需要确定一个关于哪些实例 \( x \) 实际上被标记为恶意的最优策略。

#### 攻击者模型

攻击者的效用函数是==根据成功绕过防御的概率来衡量的==。攻击者选择一个攻击向量 \( x' \) 的效用为：

$$
\Upsilon(x, x', q) = V(x) Q(x, x')(1 - q(x')),
$$

其中，\( Q(x, x') \) 是距离函数，表示攻击者在特征空间中选择新输入的代价。攻击者的目标是最大化其效用，这可以表示为一个优化问题：

$$
v(x | q) = \max \Upsilon(x, x', q).$$

#### 防御者的效用函数

为了定义防御者的目标，我们可以表示其效用函数 \( U_D(q, p) \) 为：

$$
U_D(q, p) = \mathbb{E}_x\left[(1 - q(x))(1 - p(x)) - p(x)v(x | q)\right].$$

这个函数的目标是最大化被正确分类为良性的流量期望值，减去成功绕过防御的攻击带来的预期损失。

### 5.5.2 最优随机化分类操作

在 Stackelberg 博弈模型框架下，我们可以描述解决这一问题的算法方法。首先，使用训练数据的样本平均效用来近似 \( U_D \)。然后，使用线性规划（LP）来最大化这一效用：

- 目标：最大化 \( U_D(q, p) \)
- 约束条件：定义在特征空间上的决策策略，确保攻击者的最佳响应。

然而，这种线性规划的实际应用面临两个主要问题：（1）\( q(x) \) 需要在整个特征空间上定义；（2）约束条件的数量随着特征的增加而急剧增加。

#### 基础函数表示

为了解决可扩展性问题，可以使用一组标准化的基础函数 \( \phi_j(x) \) 来表示 \( q(x) \)，即：

$$
q(x) = \sum_j \alpha_j \phi_j(x).$$

这种方法使我们能够优化 \( \alpha_j \)，从而简化问题。线性规划的形式变为：

- 目标：最小化与基础函数相关的损失。
- 约束：根据攻击者的最佳响应调整约束条件。

#### 算法 5.4：最优随机分类

该算法的核心在于使用 MASTER 线性规划来计算攻击者的（近似）最佳响应。算法的步骤如下：

1. 初始化特征矩阵。
2. 计算当前策略下的最优解 \( q \)。
3. 对每个恶意实例 \( x \)，计算其对当前策略的最佳响应 \( x' \)。
4. 更新特征集合并重复计算。

这个过程会持续进行，直到无法生成新的约束（即所有恶意实例的最佳响应已经被考虑）。

### 关键问题

1. **基础构建**：如何有效构建基础函数，以保障近似的质量。
2. **攻击者最佳响应计算**：需要一个能够计算攻击者最佳响应的“神谕”，以确保算法的有效性。

### 总结

决策随机化提供了一种新的策略，以增强模型对对抗性攻击的鲁棒性。通过将预测与决策分开，使用随机化策略和线性规划，能够有效地应对复杂的攻击者行为。这种方法在理论上和实践中都显示出了良好的适应性。

## 5.6 对抗鲁棒回归

在本章的最后，我们讨论一种开发对抗鲁棒回归的方法。这一方法基于第 4 章中讨论的回归决策时间攻击，特别是 Grosshans 等人于 2013 年提出的研究。

#### 关键概念

1. **对抗攻击**：这是针对回归模型的一种策略，攻击者试图通过调整输入数据，使得模型的预测结果出现偏差。理解这种攻击方式是设计鲁棒回归模型的基础。

2. **Stackelberg 平衡策略**：在这个框架中，学习者（防御者）首先做出决策，之后攻击者根据学习者的决策来选择他们的攻击策略。这种博弈模型帮助我们理解如何在面对对抗性攻击时优化模型。

3. **经验风险最小化**：这是防御者在设计模型时的目标，旨在通过最小化预测误差来增强模型的鲁棒性。防御者不仅关注模型的准确性，还需要考虑潜在的对抗攻击。

4. **损失函数与正则化**：在回归模型中，结合损失函数（评估预测与实际值之间的差距）和正则化项（防止模型过拟合）是至关重要的。这种结合有助于提高模型在对抗性环境中的表现。

#### 优化特性

尽管所提出的问题可能不是凸的，但它是平滑的，这意味着我们可以使用非线性编程技术来找到接近最优的解。这种方法使学习者能够调整模型参数，从而提高其对抗攻击的鲁棒性。

#### 总结

对抗鲁棒回归的方法强调了在模型设计中考虑对抗攻击的重要性。通过理解攻击者的行为和相应的防御策略，学习者可以构建出更为可靠和稳健的回归模型。