---
date: 2024-10-17
tags:
  - AdML
  - uris
  - polyu
---
## 数据中毒攻击概述

在本文中，我们探讨了一类被称为**数据中毒攻击**的攻击方式，这些攻击直接针对学习算法，==通过操纵训练数据来实现==。这些攻击与**决策时间攻击**不同，后者发生在模型训练后并处于使用状态时。

### 数据中毒攻击的分类

1. **标签修改攻击**
   - 攻击者仅修改==监督学习数据集中的标签。==
   - 通常受到总修改成本的约束。
   - 通常与二元分类器相关，通常被称为==**标签翻转攻击**。label flipping attack.==

2. **毒素插入攻击**
   - 攻击者可以添加==有限数量的任意毒素特征向量。==
   - 对标签的控制可能根据特定威胁模型而有所不同。
   - 在无监督学习中，标签不存在，只允许污染特征向量。

3. **数据修改攻击**
   - 攻击者可以==修改训练数据中任意子集的特征向量和/或标签。==

4. **慢性毒药攻击**
   - 假设防御者会迭代地重新训练模型。
   - 攻击者在每==次重新训练中注入少量毒素==，从而在时间上产生显著影响。
   - 适用==于监督和无监督设置，通常在无监督背景下进行研究。==

### 本章重点

- 讨论特定==于二元分类器的数据中毒攻击==，包括==标签翻转和毒素数据插入。==
- 探索对无监督方法如==聚类、PCA和矩阵补全的数据中毒攻击。==
- 将数据中毒攻击与机器教学联系起来，概述此类攻击的一般框架。
- 最后一节讨论黑箱数据中毒攻击。

## 6.1 数据中毒攻击建模

- 数据中毒攻击始于一个干净的数据集 $D_0$，并将其转化为 $D$，然后学习算法在 $D$ 上进行训练。
- 攻击者可能追求==**目标攻击**或**可靠性攻击**==来操纵预测。
- 攻击者的优化问题涉及平衡他们的恶意目标和修改成本，表示为：

  $$
  \min R_A(D; S) + \lambda c(D_0; D)
  $$
- 攻击者的效用也可以定义为：
  $$
  U_A(D; S) = -R_A(D; S)
  $$
### 示例
![[Pasted image 20241017153908.png]]
数据中毒的一个例子是，通过引入一个有毒的数据点（红色圆圈），使原始数据（蓝色圆圈）所学习的真实模型发生显著变化。

## 6.2 对二元分类的poisoning attack

一些关于数据中毒攻击的成熟文献主要集中==在二元分类问题上==。
### 6.2.1 标签翻转攻击

==最基本的一个数据中毒攻击是改变训练数据中一部分数据点的标签==。攻击者在此攻击中的目标通常是最大化未经修改的训练数据的错误（即修改前的数据）；在我们的术语中，这是一种==可靠性攻击==。标签翻转攻击的常见动机是，安全数据集可能是外部标记的（例如，可以使用众包来获取钓鱼邮件数据的标签），因此攻击==只能污染收集的标签，而不能污染特征向量。==

设 $D_0 = \{(x_i, y_i)\}$ 为原始的“干净”训练数据集。假设攻击者有一个标签翻转预算 $C$，改变数据点 $i$ 标签的成本为 $c_i$。设 $z_i = 1$ 表示决定翻转数据点 $i$ 的标签，$z_i = 0$ 表示决定不翻转该标签。因此，
#### 攻击者的修改成本预算可以表示为：

$$
c(D_0, D) = c(z) = \sum_{i} c_i z_i \leq C
$$

设 $D = D(z)$ 为在==选择的标签翻转子集后得到的训练数据集==。在文献中常见的标签翻转攻击的最基本变体中，目标数据集仅仅是没有恶意修改的原始训练数据集，即 $S = D_0$。忽略正则化，通过直接扩展以下表达式来处理。

#### 攻击者的优化问题为：

$$
\max U_A(D(z)) \quad \text{s.t. } \sum_{(x,y) \in D(z)} l(y \mid f(x_i; D(z))) \leq 0
$$

接下来，我们可以通过考虑一个新的数据集 $D_0$ 来等效地表示这个数学程序，其中每个数据点 $x_i$ 被复制，同时相应的 $y_i$ 被翻转，使得 $y_{i+n} = -y_i$ 对所有 $i$ 成立，并且 $D_0$ 具有 $2n$ 个数据点。设 $q_i \in \{0,1\}$ 表示选择哪个数据点，因此对于所有 $i$，恰好一个 $q_i$ 和 $q_{i+n}$ 等于 1。优化问题可以重写为：

$$
\min \sum_{i=1}^{n} (q_i \cdot [l(y_i \mid f(x_i; D_0)) - l(y_i \mid f(x_i; D_0))]) + c_i q_i \leq C \quad \text{s.t. } q_i \in \{0,1\}
$$

#### 特定于线性支持向量机的标签翻转攻击

我们将此问题专门化为线性支持向量机的标签翻转攻击。首先，观察到 $f(x_i; D_0)$ 和与每个数据点 $(x_i, y_i) \in D_0$ 相关的损失可以被预先计算。设 $\epsilon_i$ 为相应的（固定）损失。

问题变为：

$$
\min \sum_{i=1}^{n} (\epsilon_i - \xi_i) + \|w\|^2 \quad \text{s.t. } \sum_{i=1}^{2n} c_i q_i \leq C
$$

==这导致一个整数二次规划问题。优化的一个方法是交替最小化，在每次迭代中交替两个子问题：==

1. 固定 $q$ 并对 $w$ 和 $\xi$ 最小化。这成为线性 SVM 的标准二次规划问题。
2. 固定 $w$ 和 $\xi$，对 $q$ 最小化，这是一个整数线性规划问题。

### 6.2.2 毒素插入攻击ON KERNEL SVM

虽然标签翻转攻击是研究机器学习模型中毒的一种自然起点，但另一类重要的攻击是==当攻击者可以插入一组特征向量对应的数据点，但不控制这些数据点分配的标签==。例如，考虑一个垃圾邮件发送者，他可以选择发送的垃圾邮件类型，并意识到未来这些垃圾邮件可能会用来训练分类器以自动检测垃圾邮件。为了简化讨论==，假设攻击者仅向训练数据集中插入一个数据点；我们可以通过让攻击者一次插入多个数据点来概括接下来的方法。==

考虑一个原始（未修改的）训练数据集 $D_0$，然后通过添加一个实例 $(x_c, y_c)$ 来修改它，其中攻击者可以选择特征向量 $x_c$ 但不能选择标签 $y_c$，导致新数据集 $D$ 的形成。==设攻击者希望最大化学习者风险的目标数据集记为== $S$。为简化讨论，假设 $S = (x_T, y_T)$，即攻击者希望在一个目标数据点上诱导错误。观察到模型学习所需的训练数据集现在变为 $D(x_c) = D_0 \cup (x_c, y_c)$。此外，==通过允许攻击者仅添加一个特征向量（带有给定标签）到数据中，我们有效地施加了插入单个数据点的预算限制==；因此，此处无需进一步明确讨论修改成本。

设 $f_{x_c}(x)$ 为在 $D(x_c)$ 上学习到的函数。
#### 攻击者的优化问题可以表述为：

$$
\max U_A(x_c) \quad l(y_T \mid f_{x_c}(x_T))
$$

#### 针对支持向量机的攻击

如上所述，我们现在具体说明这一攻击，允许任意核函数。首先，我们引入一些新符号。对于训练数据中的数据点 $(x_i, y_i)$，定义 $Q_i(x, y) = y_i - y K(x_i, x)$，其中 $K(\cdot, \cdot)$ 为某个核函数。特别地，对于 $(x_T, y_T)$，这变为 $Q_T = y_T - y_T K(x_i, x_T)$。正如Cauwenberghs和Poggio[2001]所示，SVM损失函数可以表示为：

$$
l(y_T \mid f_{x_c}(x_T)) = \max(0, 1 - y_T f_{x_c}(x_T)) = \max(0, g_T)
$$

其中，

$$
g_T = \sum_{i \in D_0} Q_i^T z_i(x_c) + Q_c^T(x_c) z_c(x_c) + y_T b(x_c) - 1
$$

其中 $z_i$ 和 $b$ 为核支持向量机的对偶解（$b$ 也是偏置项，这里我们显式表示）。然后，

$$
f_{x_c}(x) = \sum_{i} z_i(x_c) y_i K(x_i, x) + b(x_c)
$$

Biggio 等人[2012]提出的解决该问题的方法是使用梯度上升，根据最优 SVM 解的特征来推导梯度。

#### 梯度上升方法的挑战 ...
## 6.3 无监督学习中的中毒攻击

与监督学习不同，无监督学习涉及一个仅包含特征向量的数据集 $D = \{x_i\}$。如前所述，原始“干净”的数据集记作 $D_0$，而 $D$ 指的是经过转换的数据集（可能包含新的数据点）。

==在中毒攻击的背景下，特别关注以下三种无监督学习问题：聚类（例如，用于聚类恶意软件）、异常检测和矩阵补全。==本节讨论前两种攻击，下一节将深入讨论对矩阵补全的攻击。

### 6.3.1 聚类中的中毒攻击

==聚类算法可以一般地表示为一个映射== $f(D)$，其输入为数据集 $D$，==输出为聚类分配==。许多聚类算法可以用一个矩阵 $Y$ 来表示聚类分配，其中条目 $y_{ik}$ 表示数据点 $i$ 被分配到聚类 $k$ 的概率。在大多数常见聚类算法中，$y_{ik}$ 为二元值，指示数据点的聚类分配。设原始数据集 $D_0$ 的聚类分配为 $Y_0$，==经过中毒的数据集 $D$ 的聚类分配为 $Y = f(D)$。==为了简化讨论，假设攻击者仅修改 $D_0$。

#### 攻击者的两个目标：
目标攻击和可靠性攻击。在目标攻击中，攻击者有一个目标聚类 $Y_T$，希望尽可能接近该目标。==特例是隐蔽攻击stealthy attack，其中特定目标数据点被错误聚类，而其他数据点的聚类分配不变。==在可靠性攻击中，攻击者希望最大程度地扭曲基于原始数据的聚类分配。

#### 具体
为了制定攻击者或学习者的有效成功度量，==我们需要考虑特定聚类标识是完全任意的==。实际上，非任意的是特征向量被分配到同一聚类的联合分配。我们可以通过使用 $O_0 = Y_0 Y_T^T$ 来捕捉这一点，而非 $Y_0$ 作为结果度量。因此，如果 $i$ 和 $j$ 在同一聚类 $k$ 中，即 $y_{0ik} = y_{0jk} = 1$，则 $O_0[i,j] = 1$。类似地，我们定义 $O = YY_T^T$ 为中毒攻击诱导的聚类的结果（数据点对被分配到同一聚类），$O_T = Y_T Y_T^T$ 为目标结果，表示在目标攻击中。

我们可以通过赋予攻击者一个风险函数 $R_A(O_0, O)$ 来正式建模任一攻击，其中原始和诱导的分配作为参数。对于目标攻击，自然的风险函数是诱导聚类结果与目标结果之间的距离：

$$
R_A(O_0, O) = \|O - O_T\|_F
$$

同样，对于可靠性攻击，风险函数可以是正确结果与诱导结果之间的相似度（负距离）：

$$
R_A(O_0, O) = -\|O - O_0\|_F
$$

虽然攻击者旨在改变聚类结果，但他们也面临与攻击相关的成本和/或约束。我们可以通过以下成本函数捕捉修改训练数据的成本：

$$
c(D_0, D) = c(X_0, X) = \|X_0 - X\|_F
$$

这引发了对聚类攻击的两种替代性表述。第一种最小化损失和成本的组合：

$$
\min R_A(O_0, O(X)) + \lambda c(X_0, X)
$$

其中我们明确了中毒后聚类结果对中毒数据集 $X$ 的依赖。第二种最小化攻击者风险，满足成本约束：

$$
\min R_A(O_0, O(X))
$$

$$
\text{s.t. } c(X_0, X) \leq C
$$

一般来说，聚类中的中毒攻击在计算上是极具挑战性的，因为聚类本身就是一个高度非平凡的优化问题。然而，文献中已提出几种此类攻击的重要特例，其中有效的启发式方法被提出。第一个是可靠性攻击，其中我们希望向数据集中添加 $C$ 个数据点，以最大程度地扭曲原始聚类分配，在聚合聚类的背景下[Biggio et al., 2014a,b]。这个想法是每次添加一个数据点，以便在一对相邻聚类之间架起桥梁。具体地，对于 $k$ 个聚类，我们可以定义 $k - 1$ 条从每个聚类到其最近邻聚类的桥梁。对于相应的点对，我们可以定义最短桥梁为属于不同聚类的任意两个点之间的最短距离。将一个位于这两者之间的数据点添加进去，将最有可能使这两个聚类合并。因此，通过迭代添加点，我们可以显著扭曲原始的聚类分配。

第二个特例是目标攻击，其中我们针对特定的数据点集合，希望诱导错误聚类，而不影响任何其他数据点的聚类分配[Biggio et al., 2014b]。假设 $x_i$ 是攻击者希望转移到不同聚类的特征向量，$d$ 是目标聚类中离 $x_i$ 最近的点。如果我们的成本预算约束是没有数据点的 $l_2$ 范数可以被修改超过 $C$，我们可以将 $x_i$ 转换为 $x_i + \alpha (d - x_i)$，其中 $\alpha = \min(1, \frac{C}{\|d - x_i\|_2})$。

### 6.3.2 异常检测中的中毒攻击 ANOMALY DETECTION

#### 在线中心异常检测的攻击 Online Centroid Anomaly Detection

我们讨论的第一个攻击是对中==心异常检测的攻击，其中均值是在线计算的==，源自 Kloft 和 Laskov [2012]。他们的攻击属于我们在章节开头提到的“煮青蛙”攻击类别。具体而言，他们假设异常检测器在收集新数据后定期重新训练，==而攻击者在每次重新训练迭代之间添加数据点。==
##### 具体
在此攻击中，攻击者有一个目标特征向量 $x_T$，希望在未来使用，并希望确保其被错误分类为正常。正式地，目标是使得在某次学习迭代 $t$ 时 $||x_T - \mu_t|| \leq r$，其中 $\mu_t$ 是中心均值，$r$ 是中心异常检测器的阈值（详见第2章，第2.2.4节）。在我们的术语中，这是一种目标攻击，攻击者的目标由目标中心均值 $\mu_T$ 表示，使得 $||x_T - \mu_T|| = r$。

Kloft 和 Laskov [2012] 提出了一种贪婪最优策略，用于逐步中毒在线基于中心的异常检测器。在这种策略下，在每次攻击者能够将攻击实例插入训练数据的迭代中，他们沿着当前中心 $\mu_t$ 与攻击目标特征向量 $x_T$ 之间的连线插入一个实例，并且正好位于正常区域的边界上。正式地，假设攻击者可以在迭代 $t$ 中插入毒素，并定义 $d = x_T - \mu_t$ 为从均值 $\mu_t$ 到 $x_T$ 的单位方向。这一迭代中的贪婪最优攻击为 $x = \mu_t + \alpha d$，它最大限度地向攻击目标位移中心，但仍然保持在正常区域，以确保其不会被当前异常检测器简单地丢弃。

#### 基于 PCA 的攻击

接下来，我们描述对基于 PCA 的异常检测器的攻击示例（详见第2章，第2.2.4节），来源于 Rubinstein 等人[2009]。在这种情况下，==攻击者旨在执行拒绝服务（DoS）攻击==，这意味着==添加看起来异常的流量==。在我们的表示中，这对应于向原始流量添加一个量 $i z$，其中 $i$ 是攻击者的强度，$z$ 是特征级影响。如果我们假设攻击者知道相应的未来背景流量 $x$，则攻击者需要将结果流量 $x$ 扭曲为 $x' = x + i z$，==以成功执行 DoS 攻击。攻击者的目标是通过扭曲或拉伸异常检测器，使未来的攻击看起来是正常的，来最大化== $i$。
##### 具体
在中毒攻击中，假设攻击者可以通过将一个矩阵 $X_Q \in X$ 添加到原始数据集 $X_0$ 来修改原始数据的内容，并且约束条件为 $\|X_Q\|_1 \leq C$，其中 $C$ 是攻击者的预算约束，$X$ 是可行修改的集合。设 $r$ 为异常检测器的阈值。然后，我们可以将攻击者的优化问题表示为：

$$
\max i, \; X_Q \in X \quad \text{s.t. } \|i\|_V = PCA(X + X_Q) \quad \|I - VV^T\|(x + i z) \leq r \quad \|X_Q\|_1 \leq C
$$

虽然这个问题本身是难以处理的，但我们可以近似地将目标表示为最大化攻击的投影方向的大小，即 $\|X_0 + X_Q\|_2$，yielding

$$
\max \|X_0 + X_Q\|_2 \quad X_Q \in X
$$

$$
\text{s.t. } \|X_Q\|_1 \leq C
$$

该问题反过来可以使用投影梯度上升法求解（也称为投影）。
## 6.4 矩阵补全中的中毒攻击

### 6.4.1 攻击模型概述

==在矩阵补全问题中，我们通常处理一个 $n \times m$ 的矩阵 $M$，其中行代表用户，列代表物品。攻击者可以通过添加恶意用户来影响这个矩阵，目的是操控系统的推荐结果==。

#### 攻击模型细节

- **恶意用户添加**：攻击者可以添加 $b \cdot n_c$ 个恶意用户，每个用户最多可以对 $C$ 个物品进行评分，评分范围在 $[-\varphi, \varphi]$ 之间。
- **矩阵表示**：设 $M_0$ 为原始矩阵，$M_e$ 为包含恶意用户评分的矩阵。==攻击的目标是通过优化 $M_e$ 来最大化对协同过滤系统的影响。==

#### 攻击目标

1. **可靠性攻击**：
   - ==目标是最大化协同过滤系统的错误预测。攻击者的风险函数通过未见条目的预测差异来衡量。==
   - 公式：$$U_{rel}(M_0, M_b) = \|\mathcal{R}_C(M_b - M)\|_F^2$$

2. **目标攻击**：
   - ==攻击者希望提升或降低特定物品的受欢迎程度。通过一个权重向量 $w$ 来指定每个物品的目标。==
   - 公式：$$U_{targeted}(M_b, M_0) = \sum_{i=1}^n \sum_{j \in S} w_j M_{b,ij}$$

3. **混合攻击**：
   - ==结合可靠性和目标攻击，允许攻击者在两者之间进行权衡。==
   - 公式：$$U_{hybrid}(M_0, M_b) = \lambda_1 U_{rel}(M_0, M_b) + \lambda_2 U_{targeted}(M_0, M_b)$$

#### 实用算法

为了实现这些攻击目标，研究者提出了多种优化算法，包括：

- **交替最小化方法**：用于求解最优模型参数，确保恶意用户的评分能够有效影响系统的预测。
- **投影梯度上升法**：用于优化攻击策略，确保攻击者能够在预算限制内最大化其效用。

#### 设计恶意用户

攻击者还需设计恶意用户，使其行为尽可能类似于正常用户，以避免被检测。这种设计需要考虑评分的合理性和对整个推荐系统的潜在影响。

#### 总结

矩阵补全中的中毒攻击利用恶意用户的评分来影响推荐系统，攻击者可以选择不同的攻击策略，如可靠性攻击、目标攻击或混合攻击。通过优化算法，攻击者可以有效地操控系统的输出，从而达到其目的。
### 6.4.2 攻击交替最小化方法

#### 投影梯度上升法（PGA）

在这一部分中，==我们介绍了一种投影梯度上升法（PGA），用于解决方程 (6.30) 中的优化问题，特别是针对交替最小化公式 (6.28)。==

#### 更新规则

在每次迭代中，算法通过以下公式更新恶意用户的评分矩阵 $M_e^{(t)}$：

$$
M_e^{(t+1)} = \text{Proj}_M \left( M_e^{(t)} + \beta_t \nabla_{M} U(M_0, M_b) \right)
$$

- **$\text{Proj}_M(\cdot)$**：投影运算符，确保更新后的评分矩阵 $M_e$ 仍在可行区域内。
- **$\beta_t$**：第 $t$ 次迭代的步长。

#### 梯度计算

计算梯度 $\nabla_{M} U(M_0, M_b)$ 是一个挑战，因为其中一个参数涉及隐式优化。使用链式法则，我们可以将其分解为：

$$
\nabla_{M} U(M_0, M_b) = \nabla_{M} \theta(M_0, M_e) \cdot \nabla_{\theta} U(M_0, M_b)
$$

- **第二部分的梯度**比较容易计算，因为所涉及的损失函数是光滑且可微的。
- **第一部分的梯度**则较复杂，因为涉及到一个优化过程的结果。

#### 具体算法

算法的每一步都需要计算当前评分矩阵的最优解，并根据得到的梯度更新恶意用户的评分。具体步骤如下：

1. 初始化恶意用户评分矩阵 $M_e^{(0)}$，随机选择评分和评分的物品。
2. 在每次迭代中，计算当前的最优解 $\theta(M_0, M_e^{(t)})$。
3. 计算梯度 $\nabla_{M} U(M_0, M_b)$。
4. 更新 $M_e^{(t+1)}$。

### 6.4.3 攻击核范数最小化

我们还可==以将上述投影梯度上升法扩展到核范数最小化公式 (6.29) 的攻击策略==。
#### 核范数优化

核范数最小化的目标是凸的，因此可以使用常规的凸优化方法，如近端梯度下降。

- **KKT 条件**：在优化过程中，KKT 条件用于描述最优解的特性。
- 由于核范数函数在某些点并不可微，因此需要使用次微分来处理。

#### 算法实现

算法的实现步骤如下：

1. 初始化恶意用户评分矩阵 $M_e^{(0)}$，随机生成评分。
2. 在每次迭代中，计算最优解 $\theta(M_0, M_e^{(t)})$。
3. 计算梯度，并更新评分矩阵。
4. 在最后一步中找到最优的恶意用户评分矩阵 $M_e^*$。

#### 总结

==通过投影梯度上升法和核范数最小化的方法，攻击者可以有效地设计恶意用户的评分策略，从而操控推荐系统的输出。这个过程既涉及对损失函数的优化，也需要确保更新后的评分矩阵在可行范围内。==

### 6.4.4 模仿正常用户行为 MIMICKING NORMAL USER BEHAVIORS

==正常用户的评分通常并不是随机均匀分布的==。例如，某些电影的受欢迎程度显著高于其他电影。因此，恶意用户如果随机选择评分的电影，容易被通过与正常用户数据库进行 $t$ 检验来识别。==为了缓解这一问题，本节描述了一种数据中毒攻击的方法，使得生成的恶意用户评分矩阵 $M_e$ 能够模仿正常用户 $M_0$，以避免被检测，同时仍然为攻击者提供合理的效用 $U(M_0, M_b)$。==
#### 贝叶斯方法

为了解决这一问题，攻==击者可以采用贝叶斯模型，将数据中毒和隐蔽性目标结合起来==：

1. **先验分布**：定义为多元正态分布，能够反映正常用户的评分模式。通过对正常用户的评分数据进行统计，可以计算出每个物品的平均评分和方差。
   - 先验分布 $p_0(M_e)$ 反映了正常用户的评分行为，保证恶意用户的评分不会显得太过异常。

2. **似然函数**：描述在给定恶意用户评分的情况下，正常用户评分的概率。这个函数与攻击效用模型 $U(M_0, M_b)$ 相关联，攻击者可以通过调节参数来平衡攻击效果和隐蔽性。

#### 攻击策略

攻击者的目标是从==后验分布中抽样，生成评分矩阵 ==$M_e$。后验分布结合了先验知识和观察到的数据，能够在保持隐蔽性的同时，实现攻击效用的最大化。

- **参数调节**：通过调整参数 $\lambda$，攻击者可以在攻击效果和隐蔽性之间进行权衡。较小的 $\lambda$ 会使得生成的恶意评分更接近于正常用户的行为，从而降低被检测的风险，但可能会影响攻击的有效性。

#### 总结

通过贝叶斯模型，攻击者能够设计出更为隐蔽的恶意用户评分策略，模仿正常用户的评分行为。这种方法有效地结合了隐蔽性与攻击效用的考虑，提升了数据中毒攻击的成功率。


## 6.5 一种通用的数据中毒攻击框架

### 概述

本节介绍了由 Mei 和 Zhu 提出的==数据中毒攻击的通用框架==，强调攻击者如何通过修改数据集来影响学习过程。==这种方法与机器教学密切相关，适用于监督学习场景，使攻击者能够添加和修改数据以实现其目的。==

### 学习问题的框架

==在传统的监督学习中，目标是找到最佳参数化 $w$，以最小化损失函数与正则化项的组合。==这个过程可以表示为优化问题：

$$
w(D) \in \arg \max_w \sum_{i \in D} l_i(w) + \lambda(w)
$$

这里，$l_i(w)$ 代表每个数据点的损失，$\lambda(w)$ 是正则化项。由于攻击者会修改数据集 $D$，其对学习结果的影响变得尤为重要。

### 攻击者的目标

攻击者的任务是从原始数据集 $D_0$ 创建新的数据集 $D$，以最小化其风险函数 $R_A(w(D))$，同时控制成本函数 $c(D_0, D)$。==这种权衡可以形式化为一个双层优化问题：==

$$
\min R_A(w(D)) + c(D_0, D) \quad \text{s.t.} \quad w(D) \in \arg \max_w \sum_{i \in D}
$$

### 优化策略

1. **双层优化**：攻击者面临的双层优化问题要求在降低风险与控制成本之间找到平衡。这一过程不仅复杂而且要求攻击者具备良好的策略。

2. **一阶条件**：==如果学习者的问题是严格凸的，攻击者可以重写约束条件，使用一阶条件简化优化过程，从而利用投影梯度下降法进行优化。==

3. **更新步骤**：更新过程可以表示为：

$$
D^{t+1} = \text{Proj}_D \left( D^t - \beta_t \left( \nabla_D R_A(w(D)) + \nabla_D c(D, D_0) \right) \right)
$$

通过这种方式，攻击者能够逐步调整数据集以实现其目标。

### 隐式函数定理的应用

隐式函数定理的使用使得攻击者能够计算损失函数对数据集的导数，简化了优化过程。攻击者可以定义函数集合 $f_j(D, w)$ 来表示一阶条件，并利用 Hessian 矩阵进行推导，从而有效地计算所需的导数。

### 实际案例：逻辑回归攻击

在逻辑回归的具体案例中，攻击者可能只修改特征向量。攻击者的目标是通过最小化损失函数和控制特征矩阵的变化来影响模型的学习结果。攻击者的成本函数和风险函数明确表述，为实施攻击提供了理论基础。

### 总结

这一通用框架为数据中毒攻击提供了一种系统化的方法，通过结合优化理论与具体应用分析，攻击者能够灵活地修改数据集以实现其目标。这种方法强调了数据修改对学习过程的重要性，并为攻击者提供了多种实现策略，从而提升攻击的有效性。

## 6.6 黑箱数据中毒攻击

### 黑箱攻击的基本信息需求

在白箱数据中毒攻击中，攻击者需要掌握三方面的信息：
1. **特征空间** $F$：攻击者需要知道数据的特征是如何定义的。
2. **原始数据集** $D$：攻击者需要了解学习者在攻击前使用的数据集（注意，此处指的是对数据集的知识，而非数据集本身）。
3. **学习算法** $A$：包括学习者使用的算法及相关的超参数。

### 信息层次结构

图 6.2 展示了黑箱数据中毒攻击的信息层次结构，从白箱攻击（完全信息）开始。如果攻击者不清楚所用的算法，可以使用代理算法来评估在错误假设下的攻击有效性。如果特征空间未知，攻击者也可以使用代理特征空间，但这会大大限制其掌握的信息。

### 数据集信息的限制

黑箱攻击中，最显著的限制在于攻击者对被中毒数据集的信息较少。如果代理数据仅为学习者使用的部分数据，攻击者仍然可以在理论上对数据实例（包括标签）进行修改，但缺乏对训练数据的实际访问使得这些攻击难以实现。 

- **插入攻击**：攻击者可以进行插入攻击，尽管如果代理数据与学习者实际使用的数据不具有代表性，这种攻击的有效性将大打折扣。

### 总结

黑箱数据中毒攻击的实施面临多重挑战，主要源于对特征空间、算法和数据集的有限知识。虽然在一些情况下可以采用代理方法，但攻击的有效性往往依赖于代理信息的准确性和代表性。总体而言，黑箱攻击的成功实现比白箱攻击更为复杂。