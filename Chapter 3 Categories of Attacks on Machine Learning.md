---
date: 2024-10-12
aliases:
  - 机器学习攻击的分类
tags:
  - AdML
  - uris
  - polyu
---
在这一章中，我们提出了==机器学习背景下威胁模型或攻击的一般分类==。我们后续对具体攻击的详细介绍将基于这一分类。

==特别地，我们沿三个维度对攻击进行分类：时机、信息和目标。==timing, information and goals.

### 1. 时机 timing
建模攻击时第一个关键考虑因素是==攻击发生的时间==。
这个考虑导致了对机器学习攻击的一个常见二分法：==模型攻击（其中规避攻击evasion attacks 是最具代表性的案例）和算法攻击（通常称为投毒攻击）==。模型攻击或更准确地说是对==学习模型的决策的攻击==，假设==模型已经被学习==，攻击者现在要么改变其行为，要么对观察到的环境进行更改，以导致模型做出错误预测。相对而言，==投毒攻击发生在模型训练之前==，修改用于训练的一部分数据。这个区别如图 3.1 所示。

![[Pasted image 20241012210950.png|500]]

### 2. 信息
建模攻击的第二个重要问题是==攻击者对学习模型或算法的了解程度==，这一区别通常分为==白盒攻击和黑盒攻击==

### 3. 目标 Goals
攻击者可能有不同的攻击理由，例==如规避检测或降低算法的信心==。我们区分两类广泛的攻击目标：有==针对性的攻击targeted attacks==和对学习方法的可靠性攻击（或简单称为==可靠性攻击reliability attacks==）。

在有针对性的攻击中，攻击者的目标是导致特定性质实例的错误（例如，导致学习函数 $f$ 对实例 $x$ 预测特定的错误标签 $l$）。而可靠性攻击则旨在通过==最大化预测错误==来降低学习系统的感知可靠性。

在表 3.1 中，我们总结了对机器学习攻击的分类。

|   |   |
|---|---|
|Attack Timing|Decision time (e.g., evasion attack) vs. training time (poisoning)|
|Attacker Information|White-box vs. black-box attacks|
|Attack Goals|Targeted attacks vs. reliability attacks|

本书的高层组织主要集中在==模型攻击和投毒攻击之间的第一个二分法上==
# 3.1 攻击时机 ATTACK TIMING

## ==**决策时间攻击**Attacks at Decision Time==

在我们考虑的所有攻击类别中，==规避攻击==（决策时间发生的主要攻击子类）。一个众所周知的规避例子是垃圾邮件流量的演变，例如，当垃圾邮件发送者将字母“i”替换为数字“1”或字母“l”时，“Viagra”变成“V1agra”。

- **输入选择**：攻击者选择一个“理想”实例（即希望模型正确分类的实例）并确定目标标签。
- **特征修改**：通过对特征向量进行修改，生成新的输入，使得模型在接收到该输入时预测错误的标签。
- **攻击成功标准**：如果模型输出与攻击者的目标一致（例如，将垃圾邮件分类为正常邮件），则攻击成功。
### example
作为一个例子，假设我们希望检测垃圾邮件，并为此学习一个分类器 $f(x)$（其中 $x$ 是表示电子邮件特征的向量）。现在考虑一个垃圾邮件发送者，之前使用的模板对应于特征向量 $x_{spam}$，并假设 $f(x_{spam})$ 将其标记为“垃圾邮件”（$+1$），因此垃圾邮件发送者没有收到响应。垃圾邮件发送者会对电子邮件进行修改，以获得一个在特征空间中看起来像 $x_0$ 的实例，并使得 $f(x_0) = -1$（即，被分类为非垃圾邮件，能够通过用户的邮箱）。但是 $x_0$ 不能是任意的：攻击者修改原始实例 $x_{spam}$ 以达到 $x_0$ 会产生一定成本，这可能涉及努力的成本（以维持功能性）或有效性（例如，他们可能需要引入拼写错误，以避免被检测，但这也会降低人们点击嵌入链接的可能性）。

### 推广到多类分类
将规避攻击的思想推广到多类分类中。设 $Y$ 为有限标签集，假设对于某个实例 $x_{ideal}$，预测标签为 $f(x_{ideal}) = y$。攻击者可能希望将此实例更改为另一个实例 $x_0$，以便造成错误预测（$f(x_0) \neq y$）或使分类器预测目标标签 $t = f(x_0)$。最近，这类攻击受到极大关注，==通常称为对抗性示例==，主要集中在==视觉应用和深度神经网络上==。一个潜在的担忧是，攻击者可能试图通过操纵对道路标志的感知图像（例如，通过张贴看似涂鸦的特殊贴纸）来导致依赖视觉的自动驾驶车辆发生碰撞 [Evtimov et al., 2018]。虽然这个问题是决策时间攻击的特例，但它所受到的独立关注程度值得设立一个单独的章节（第 8 章）。

## 训练数据的攻击

==使用被损坏或噪声训练数据==进行学习的问题在机器学习和统计学界已经研究了几十年。然而，对==训练数据的对抗性损坏==最近得到了更系统的考虑，特别是当我们允许一定比例的数据被损坏时。投毒攻击的性质是，攻击者在训练之前故意操纵训练数据，以使学习算法做出不良选择。==投毒攻击的一个重要概念挑战在于定义对训练数据的对抗性操纵的范围，==以及==攻击者这样做的目标==。

还可以考虑更具体的损坏模型==，对攻击者可以采取的行动施加额外约束==。这样攻击的一类常见形式是==标签翻转攻击label-flipping attacks==，其中攻击者可以改变训练数据中至多 $C$ 个数据点的标签。通常，此类攻击在分类背景下进行考虑。在大多数情况下，假设算法和特征空间对攻击者是已知的（即，白盒攻击）。==数据投毒也可以在无监督学习环境==中考虑

# 3.2 攻击者可获得的信息

在攻击建模中，攻击者对所攻击系统的信息是一个至关重要的因素。我们区分两种攻击类型：白盒攻击和黑盒攻击。

## 白盒攻击
白盒攻击假设攻击者对学习模型（例如，实际分类器）或学习算法有完全的了解。这意味着攻击者知道所有模型参数，包括特征，以及在投毒攻击中学习算法的超参数。
## 黑盒攻击
相对于白盒攻击，黑盒攻击则假设攻击者对学习者使用的模型或算法没有精确的信息。黑盒攻击的一个重要建模挑战是==准确建模攻击者对学习模型或算法的知识。==

在决策时间的黑盒攻击中，可以考虑攻击者对学习模型的信息层次结构：
- **无信息**：攻击者没有任何信息。
- **有限信息**：攻击者可能有==一些与实际模型训练数据不同的训练数据==，但对正在学习的模型类别或使用的特征没有信息。
- **更有信息**：攻击者知道模型类别和特征，甚至可能知道学习算法，==但没有训练数据==。更有信息的攻击者还可能拥有与学习时使用的==数据分布相同的训练数据。==
- **白盒攻击等价**：如果攻击者获得了学习算法==实际使用的训练数据==，那么攻击者就可以从这些数据中学习出确切的模型。

## 查询模型 query

### 概念
在决策时间的黑盒攻击模型中，攻击者可能通过查询访问学习模型来获取信息。具体而言，给定一个特征向量 $x$，攻击者可以获得未知黑盒模型 $f$ 的实际预测标签 $y = f(x)$。通常，这种查询模型==假设攻击者知道模型空间（例如，学习算法）和特征空间==。然而，这种模型的一个实际限制是，给定 $x$，$f(x)$ 的观察往往是==不精确或有噪声==的。

### 例子
例如，假设垃圾邮件发送者发送了一封垃圾邮件，未响应并不一定意味着该邮件被过滤，而可能是用户简单地忽视了该邮件。

## 数据投毒攻击
类似于==决策时间的黑盒攻击==，黑盒数据投毒攻击允许攻击者对防御者使用的算法有不同程度的了解。这种信息层次结构有助于理解攻击者在实施攻击时所面对的不同信息情境，进而影响攻击的策略和效果。

# 3.3 攻击者目标

攻击者针对机器学习系统实施攻击的可能目标非常广泛，但我们将攻击简化为两大主要类别：==有针对性的攻击和可靠性攻击。targeted attacks and reliability attacks.==

## 有针对性的攻击
有针对性的攻击的特征在于==攻击者对模型决策有特定的目标==。在监督学习的最常见设置中，攻击者希望诱导每个 $(x, y) \in S$ 的预测遵循目标标签函数 $l(x)$。

## 可靠性攻击
另一方面，可靠性攻击==试图最大化学习在真实值上的决策错误==。例如，在监督学习中，攻击者的目标是最大化预测错误。在视觉应用中，这类攻击通常被称为无目标攻击，旨在修改图像以导致错误的预测（例如，将停止标志错误识别为其他任何路标）。

## 攻击分类的模糊性
我们在有针对性攻击和可靠性攻击之间的区分在考虑==二元分类时会变得模糊==