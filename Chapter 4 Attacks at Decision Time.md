---
date: 2024-10-13
aliases:
  - 决策攻击
tags:
  - AdML
  - uris
  - polyu
---
在本章中，我们开始考虑机器学习中的决策时间攻击。如前所述，这个问题的==典型例子是对垃圾邮件、网络钓鱼和恶意软件检测器的对抗性规避==，这些检测器经过训练以区分良性实例和恶意实例，攻击者通过操控对象的特性，例如引入巧妙的拼写错误或代码区域的替换，以使其被错误分类为良性。

### 挑战

==分析学习方法对决策时间攻击的鲁棒性时，关键挑战在于建模这些攻击==。在本章中，我们介绍决策时间攻击的常见==数学模型==，以及解决计算最佳（或近似最佳）攻击的非平凡问题的相关==算法方法==。

### 模型讨论

首先讨论白盒攻击，然后讨论黑盒攻击。此外，我们描述许多主要学习算法类的白盒攻击的常见模型：从对二元分类器的决策时间攻击（通常称为规避攻击）开始，我们将模型推广到多类分类器，然后继续描述对异常检测、聚类、回归以及最后的强化学习的攻击。

# 4.1 决策时间攻击示例

我们将从网络安全文献中开始介绍几个规避攻击的示例——决策时间攻击的重要子类。在规避攻击中，学习模型用于检测恶意行为，例如入侵或恶意可执行文件，攻击者旨在改变攻击特征以保持未被检测。
### 多态混合攻击 polymorphic blending attack

第一个示例是多态混合攻击，目标是基于统计异常检测的入侵检测系统（IDS）intrusion detection system based on statistical anomaly detection。在这种攻击中，恶意软件通过引入变体，以逃避检测。

### PDF 恶意软件分类器的规避攻击

第二组示例涉及对便携式文档格式Portable Document Format（PDF）恶意软件分类器的规避攻击。我们描述几种此类攻击的实例：

1. **模仿攻击**：这种攻击试图将良性特征引入恶意文件，而不是去除看起来恶意的特征。
2. **自动化攻击**：这种攻击能够自动添加和移除 PDF 对象，从而更有效地规避检测。

## 4.1.1 对异常检测的攻击：多态混合攻击 POLYMORPHIC BLENDING

多态混合攻击是一种针对基于异常检测的入侵检测系统（IDS）的攻击方式，旨在通过改变恶意软件的特征，使其看起来更像正常流量，从而逃避检测。
### 解释
考虑异常检测系统的一种通用方法是==将被建模的实体（例如IDS中的网络流量）转换为数字特征向量，例如x。==一种常见的方法是==使用 n-gram==

我们可以==获得“正常”流量的数据集==，==并对相关特征向量的分布进行建模==。如果我们添加一个似然阈值，则可以标记任何似然（给定正常数据的分布）低于该阈值的数据包。

一个简单（且相当常见）的方法是==计算正常流量特征向量的均值 $\mu$，并对均值施加阈值，使得任何 $x$ 满足 $||x - \mu|| > r$ 的数据包被标记为异常==（其中 $r$ 被选择以实现目标低假阳性率）。

现在我们可以简要描述多态混合攻击。这些攻击的目标是==生成恶意软件的多态实例（视为数据包序列），同时实现尽可能接近正常的特征表示。==
- **加密**：将正常数据包中常见的字符替换为不常见的字符，以混淆检测。
- **解密**：在执行时，使用反向映射确保只使用正常的 n-grams（连续字节序列）。
- **填充**：向数据包中添加正常字节，使其特征向量更接近正常流量的特征。

## 4.1.2 对 PDF 恶意软件分类器的攻击

为了讲解 PDF 恶意软件分类及相关攻击，我们首先简要介绍 PDF 文档结构。

### PDF 结构
PDF（便携式文档格式）是一种开放标准格式，用于在不同平台上呈现内容和布局。PDF 文件结构由四个部分组成：
- **头部**：包含格式版本等信息。
- **主体**：PDF 文件中最重要的部分，由多个对象构成文件内容。每个对象可以是八种基本类型之一：布尔值、数字、字符串、空值、名称、数组、字典和流。此外，对象可以通过间接引用相互引用。
- **交叉引用表（CRT）**：索引主体中的对象。
- **尾部**：指向 CRT。

PDF 文件的解析从尾部开始，尾部包含 CRT 的位置，然后直接跳转到 CRT，使用 CRT 中的对象位置信息解析 PDF 文件的主体。

### PDFRate PDF 恶意软件检测器
==PDFRate 分类器==由 Smutz 和 Stavrou 开发，使用随机森林算法，利用 PDF 元==数据和内容特征 PDF metadata and content features ==对良性和恶意 PDF 文件进行分类。元数据特征包括文件大小、作者姓名和创建日期，而基于内容的特征包括特定关键词的位置和计数。

### 对 PDFRate 恶意软件检测器的攻击示例
我们现在简要描述对 PDFRate 的两种攻击。这些攻击利用 PDFRate 的特征提取机制中的线性处理方式。
...

# 4.2 决策时间攻击的建模

在深入探讨这些攻击的几种自然模型的数学细节之前，我们首先从概念上描述这些模型，并在过程中澄清一些术语。
### 决策时间攻击的定义
决策时间攻击是==一种针对机器学习模型的攻击，而不是针对算法的攻击==。例如，线性支持向量机和感知器算法都生成线性分类器 $f(x) = \text{sgn}(w^T x)$，其中 $w$ 是特征权重。从决策时间攻击的角度来看，我们只关心最终结果 $f(x)$，而不关心产生该结果的算法。然而，在讨论攻击时，模型的结构才是相关的。

### 攻击目标
在典型的决策时间攻击中，攻击者旨在实现两个目标：
1. 完成恶意目标，例如危害主机。
2. 显著降低被学习模型标记为恶意的可能性。
3. 
相关的次要目标是攻击者尽量减少成功攻击所需的努力。

### 示例
**示例 4.1**：考虑一个针对二元分类器的对抗性规避攻击，该分类器将实例（例如，垃圾邮件）标记为恶意或良性。在这个例子中，我们有一个单一特征，称为 $x$。我们使用基于得分的分类器 $f(x) = \text{sgn}(g(x))$，其中 $g(x) = 2x - 1$。换句话说，当 $g(x) \geq 0$（即 $x \geq 0.5$）时，实例 $x$ 被分类为垃圾邮件；否则，被分类为非垃圾邮件。

假设垃圾邮件发送者创建了一封特征值为 $x_{\text{spam}} = 0.7$ 的垃圾邮件。与之相关的 $g(x_{\text{spam}}) = 0.4 > 0$。在规避攻击中，垃圾邮件发送者会更改垃圾邮件，使得其对应的数值特征 $x'$ 下降到 $0.5$ 之下，从而确保 $g(x') < 0$。换句话说，特征值为 $x'$ 的垃圾邮件现在被分类为非垃圾邮件。

在本章的其余部分，我们将==描述决策时间攻击的常见建模和数学分析方法。==
![[Pasted image 20241013171249.png]]

# 4.3 白盒决策时间攻击

在建模攻击时，==一个主要挑战是攻击者对学习模型掌握的信息==。我们暂时搁置这个讨论，假设攻击者知道被攻击的模型；也就是说，我们首先考虑白盒攻击。

## 4.3.1 对二元分类器的攻击：对抗性分类器规避

白盒规避攻击对二元分类器的常见抽象始于三个构造。==第一个是分类器 $f(x) = \text{sgn}(g(x))$，其中 $g(x)$ 是某个评分函数==。

第二个构造是与攻击者希望使用的行为或对象的特征特征对应的==对抗性特征向量 $x_A$。==此后，==我们称 $x_A$ 为理想实例，==
	因为这是攻击者如果未被分类器 $f(x)$ 标记为恶意时所使用的攻击向量。

==第三个构造是成本函数 $c(x, x_A)$，它为特征向量 $x$ 所描述的攻击分配一个成本==。这个成本旨在==捕捉将理想实例 $x_A$ 修改为 $x$ 的难度==，
	这可能来自任何来源，包括恶意功能的任何降低。规避攻击将==旨在平衡两个考虑因素==：对分类器表现良好（由 $g(x) \leq 0$ 或 $f(x) = -1$ 捕捉），以及最小化成本 $c(x, x_A)$。注意，对抗性规避在我们的术语中根本上是一种有针对性的攻击，因为攻击者希望特定的恶意
### 攻击模型

#### 成本函数1
建模规避成本的==最常见方法是使用 $l_p$ 距离，或其加权推广==：
$$c(x, x_A) = \|x - x_A\|_p;$$
$$c(x, x_A) = \sum_j \alpha_j |x_j - x_{A,j}|;$$
##### 解释
==其中权重 $\alpha_j$ 旨在捕捉改变特征 $j$ 的难度==。==最常用的有 $l_0$、$l_1$、$l_2$ 或 $l_{\infty}$ 范数（对应于 $p=0, 1, 2, \infty$）。==我们将所有这些成本函数的变体称为基于距离的，因为它们是基于修改后的特征向量与原始特征向量之间的距离度量（$l_p$ 范数）。

#### 成本函数2
==基于距离的成本函数的一个有趣变体是可分离成本函数：==
$$c(x, x_A) = \max\{0, c_1(x) - c_2(x_A)\};$$
该函数粗略假定==生成规避实例 $x$ 的成本与目标实例 $x_A$ 无关==（在最终成本非负的约束下）。
#### 成本函数3 -- 基于局限
##### 局限
基于距离的成本函数的一个局限性是，==它们未能捕捉真实攻击的重要特征：攻击特征之间的可替代性或等价性。==例如，使用同义词，而不会显著改变消息的语义。合理地认为，==这种特征交叉替代攻击的成本为零==。为了建模这一点，假设每个特征 $j$ 有一个等价类 $F_j$，其中的其他特征可以被“自由”用作 $j$ 的替代。

##### 成本函数
![[Pasted image 20241013172634.png]]
其中 $\oplus$ 是异或运算，因此 $x_{A,j} \oplus x_k = 1$ 确保我们只在不同特征之间替代，而不是简单地添加特征。

无论使用何种成本函数，接下来的问题是
#### 如何表示攻击者在看起来良好于分类器和最小化规避成本之间的权衡 4.6
最直观的表示方式可能是通过以下优化问题：
$$ \min \left[\min\{g(x), 0\} + \lambda c(x, x_A)\right]; $$
##### 解释
其中 $\lambda$ 是一个参数，用于==权衡看起来更良好和产生规避成本的相对重要性==。注意，$\min\{g(x), 0\}$ 表示如果它们被标记为恶意，攻击者将获得零效用，但从看起来更良好的状态中受益（即，$g(x)$ 较小）。这反映了安全文献中的典型规避攻击，其中攻击明确希望在分类器中尽可能表现良好。

##### 改进
然而，这==个项也使得优化在特征空间和 $g(x)$ 为凸的情况下变得非凸。==攻击者的一个自然凸松弛是以下替代目标：
$$ \min \left[g(x) + \lambda c(x, x_A)\right]; $$

### 另一个建模方法

另一种建模方法是假设攻击者==仅关心看起来良好，而不关心良性的程度==。这可以通过几个本质上等价的模型来捕捉。一个模型是以下==优化问题==
#### 基于优化建模
$$
\min c(x, x_A) \quad (4.7a)
$$
$$
\text{s.t. } f(x) = -1 \quad (4.7b)
$$
##### 解释
虽然这个模型直观，但它的特点是攻击者只要存在某个特征向量 $x$ 被分类器 $f(x)$ 标记为良性，就总能成功。因此，这个模型在==分析分类器脆弱性==时最有用，形式上我们还会==施加成本预算约束 $C$==。更准确地说，假设 $x^*$ 解决了问题 4.7。攻击者选择攻击特征向量 $x_{\text{new}}$ 的决策规则是：

$$
x_{\text{new}} = 
\begin{cases} 
x^* & \text{if } c(x^*, x_A) \leq C \\ 
x_A & \text{o.w.}
\end{cases} \quad (4.8)
$$

这基本上是 Dalvi 等人 [2004] 使用的模型，他们称相关问题为==最小成本伪装minimum cost camouflage.。

#### 另一种看待这个问题的方法是让攻击者解决以下优化问题：

$$
\min \left[f(x) + \lambda c(x, x_A)\right] \quad (4.9)
$$

（注意用 $f(x)$ 替代了实值的 $g(x)$，==而 $f(x)$ 是二元的==）。这个问题为攻击者提供了与我们刚刚描述的决策规则等效的解决方案，其中预算约束 $C = \frac{2}{\lambda}$。

#### 这一主题的另一个变体是攻击者的以下优化问题：

$$
\min g(x) \quad (4.10a)
$$
$$
\text{s.t. } c(x, x_A) \leq C \quad (4.10b)
$$

这同样对攻击者施加了成本预算约束，但与其专注于最小化规避成本，不如说试图让实例看起来尽可能良好。

###  攻击的变体

在这个最后的优化框架上，有几个有趣的变体考==虑了攻击者可以进行的可行修改的更复杂约束==，并将==目标替换为防御者的损失 $l(g(x))$。==
#### 第一个例子
第一个例子是==自由范围攻击free-range attack==，它假设攻击者可以在特征空间中自由移动数据。攻击者所需的唯一知识是每个特征的有效范围。设 $x_{\max}$ 和 $x_{\min}$ 为第 $j$ 个特征可以取的最大和最小值。攻击实例 $x$ 的边界如下：

$$
x_{\min} \leq x \leq x_{\max}, \quad \forall j; \quad (4.11)
$$

其中 $C_f \in [0, 1]$ 控制攻击的激进程度：$C_f = 0$ 表示没有攻击可能，而 $C_f = 1$ 则对应于涉及最大范围数据移动的最激进攻击。
#### 第二个例子
第二个例子是==受限攻击restrained attack==，它试图将初始恶意特征向量 $x_A$ 移动到特定目标 $x_t$。攻击者可以将 $x_t$ 设置为典型的良性特征向量

在大多数情况下，攻击者无法如愿将 $x_A$ 改变为 $x_t$，因为这将妨碍攻击的恶意价值。为捕捉这一直觉，受限攻击施加了几个约束，要求新的规避实例 $x$ 必须满足。
##### 几个约束
首先，有：

$$
(x - x_A) \cdot (x_t - x_A) \geq 0;
$$

这确保了修改与目标的方向相同。此外，这种攻击对属性 $j$ 的位移量施加了上限，如下所示：

$$
|x_j - x_{A,j}| \leq C_{\delta} \left(1 - C_{\iota} \frac{|x_j^t - x_{A,j}|}{|x_j^t|}\right) |x_t - x_i|; \quad (4.12)
$$

其中 $C_{\delta}, C_{\iota} \in [0, 1]$ 模拟了相对于目标的原始特征向量位移的恶意效用的相对损失。这些参数共同决定了攻击可以多么激进。项 $1 - C_{\iota}$ 限制了相对于 $x_A$ 的规避攻击的大小，基于目标和理想实例之间的原始距离：这些距离越远，攻击者可以影响的比例就越小。


### 计算最优攻击 Computing Optimal Attacks
现在我们已经定义了一些关于攻击者在规避分类器时目标的模型，接下来的问题是：==我们如何实际解决这些优化问题？==我们现在来探讨这个问题。

	首先观察，如果 $g(x)$ 和 $c(x, x_A)$ 在 $x$ 上是凸的，并且 $x \in \mathbb{R}^m$ 是连续的，上述攻击者决策问题的几乎所有表述都是凸的，因此可以使用标准的凸优化技术进行求解。作为一个简单的例子，假设
	
	$$
	g(x) = w^T x \quad \text{和} \quad c(x, x_A) = \|x - x_A\|^2_2
	$$
	
	优化问题 (4.6) 将产生一个闭合形式的解：
	
	$$
	x^* = x_A - \frac{\lambda^2 w}{2}.
	$$
	
	然而，更一般地，优化问题可能是非凸的。如果我们假设特征空间是实值的，并且目标足够光滑（例如，具有可微的成本函数和 $g(x)$），解决这些问题以获得局部最优解的最基本技术之一是梯度下降法。为了说明这一点，假设攻击者旨在优化问题 (4.6)。如果目标的梯度为
	
	$$
	G(x) = \nabla_x g(x) + \nabla_x c(x, x_A),
	$$
	
	梯度下降过程将迭代应用以下更新步骤：
	
	$$
	x_{t+1} = x_t - \beta_t G(x_t); \quad (4.13)
	$$
	
	其中 $\beta_t$ 是更新步长。如果 $g(x)$ 和 $c(x, x_A)$ 足够光滑，二阶方法如牛顿-拉夫森法也会有效 [Nocedal 和 Wright, 2006]。当然，如果攻击者的问题是凸的，这两种方法都将产生最优解。
	
	然而，特征空间通常是离散的，甚至是二元的。解决这类问题的一种简单通用方法是我们统称为坐标贪婪（CG）的方法。在 CG 中，首先随机选择特征的顺序，然后迭代尝试从可能性集合中逐一改变一个特征（我们假设这个集合是有限的，通常情况下特征是离散的），在保持其他特征不变的情况下为该特征选择最佳值。这个过程要么在固定的迭代次数后停止，要么在收敛到局部最优解时停止。
	
	即使是离散特征的问题，通常也可能具有足够的特殊结构，以容纳有效的全局优化方法。一个例子是 Dalvi 等人 [2004] 提出的最小成本伪装攻击，可以使用整数线性规划进行最优计算。为了简化讨论，我们假设在这种情况下特征空间是二元的（原始论文中的方法更为通用）。他们的攻击特定于朴素贝叶斯（NB）分类器（实际上是其成本敏感的推广）。NB 分类器计算概率 $p_C(x) = \Pr(y = C1 | x)$ 和 $p_-(x) = 1 - p_C(x)$，并当且仅当
	
	$$
	\log(p_C(x)) - \log(p_-(x)) > r
	$$
	
	对于某个阈值 $r$（例如，通过考虑假阳性和假阴性的相对重要性来获得）。由于对于 NB 分类器，有 $\Pr(y = C1 | x) = \Pr(y = C1) \prod_j \Pr(x_j | y = C1)$，同样 $\Pr(y = -1 | x) = \Pr(y = -1) \prod_j \Pr(x_j | y = -1)$，我们可以等效地重写决策为：
	
	$$
	\log \Pr(y = C1) + \sum_j \log \Pr(x_j | y = C1) - \log \Pr(y = -1) - \sum_j \log \Pr(x_j | y = -1) > r,
	$$
	
	或者
	
	$$
	\sum_j \left(\log \Pr(x_j | y = C1) - \log \Pr(x_j | y = -1)\right) > r_0; \quad (4.14)
	$$
	
	其中
	
	$$
	r_0 = r - (\log \Pr(y = C1) - \log \Pr(y = -1)). \quad (4.15)
	$$
	
	我们定义 $L_j(x_j) = \log \Pr(x_j | y = C1) - \log \Pr(x_j | y = -1)$，$L(x) = \sum_j L_j(x_j)$，以及 $gap(x) = L(x) - r_0$。$gap(x)$ 是重要的，因为它捕捉了日志赔率的最小变换，以使特征向量获得负分类，也就是说，使实例被标记为良性而非恶意。一个关键观察是，对特征的修改通过 $L_j(x_j)$ 独立影响分类决策。此外，我们可以定义翻转特征 $x_j$（将其从 1 改为 0，或从 0 改为 1）的净影响为：
	
	$$
	\Delta_j(x_j) = L_j(1 - x_j) - L_j(x_j).
	$$
	
	因此，攻击者的目标是对被标记为恶意的原始实例 $x$ 施加的总变化超过 $gap(x)$。
	
	现在我们可以将攻击者的优化问题表述为以下整数线性规划，其中 $z_j$ 是二元决策变量，用于确定是否修改特征 $j$：
	
	$$
	\min \sum_j z_j \quad (4.16a)
	$$
	$$
	\text{s.t. } \sum_{j \in A} \Delta_j(x_j) z_j \geq gap(x), \quad z_j \in \{0, 1\}; \quad (4.16b)
	$$
	
	其中 $x_{j,A}$ 是原始“理想”对抗实例 $x_A$ 中特征 $j$ 的值。这只是背包问题 [Martello 和 Toth, 1990] 的一种变体，尽管在理论上是 NP-Hard 的，但在实践中可以非常快速地解决 [Kellerer 等, 2004]。最后，一旦计算出最小成本伪装，结果将与攻击者的成本预算进行比较。攻击者仅在从原始理想实例的修改低于成本预算时实施相关操作 $x'。
	
	考虑到实现一般对抗优化问题的最优解的困难，另一种方法是针对实现良好最坏情况近似保证的算法。这样的例子相对较少，但一个值得注意的案例是 Lowd 和 Meek [2005a] 提出的算法，用于解决问题 (4.7)，并且使用统一的 $l_1$ 成本函数（即在成本函数 (4.2) 中 $\alpha_i = 1$ 对于所有 $i$）。该算法从一个任意的良性实例 $x = x_0$ 开始。然后重复两个循环，直到没有进一步的修改可能：第一个循环尝试翻转 $x$（当前特征向量）中与理想实例 $x_A$ 不同的每个特征；第二个循环尝试用当前在 $x$ 和 $x_A$ 中相同的某个特征替换与 $x_A$ 不同的一对特征（但这样会变得不同）。

## 4.3.2 决策时间攻击对多类分类器的影响

在介绍了二元分类器中决策时间攻击的基本概念后，我们现在将这些概念推广到对多类分类器的攻击。

### 定向攻击

假设攻击者旨在将理想实例 $x_A$ 转换为标记为目标类 $t$。这种攻击的一个自然且非常通用的模型是以下优化问题：

$$
\min c(x, x_A) \quad \text{s.t. } f(x) = t; \quad (4.17)
$$

我们可以像之前一样施加成本预算约束。如果我们希望考虑可靠性攻击，可以将模型 (4.17) 中的约束替换为 $f(x) \neq y$，其中 $y$ 是正确标签。

### 多类分类器的结构

通常，我们对多类分类器有更多的结构，因为它通常可以表示为：

$$
f(x) = \arg \max_y g_y(x) \quad (4.18)
$$

其中 $g_y(x)$ 是某个评分函数（注意，这直接推广了基于评分的二元分类，其中 $f(x) = \text{sgn}(g(x))$）。在这种情况下，我们可以将公式 (4.6) 转换为：

#### 成本函数

- 对于定向攻击：

$$
\max \left[g_t(x) - \lambda c(x, x_A)\right] \quad (4.19)
$$

- 对于可靠性攻击：

$$
\min \left[g_y(x) + \lambda c(x, x_A)\right] \quad (4.20) 
$$

（其中 $y$ 是 $x$ 的正确标签）。

### 问题及解决方案
#### 问题
不幸的是，这种对多类分类器的对抗性规避的推广存在问题。例如，考虑一个定向攻击，其中攻击者的目标是确保 $f(x) = t$，但是当我们仅仅最大化 $g_t(x)$ 时，可能会无意中最大化某个其他类 $y \neq t$ 的 $g_y(x)$，最终结果是对问题 (4.19) 的最优解 $x^*$ 满足 $g_t(x^*) < g_y(x^*)$。换句话说，攻击者可能无法实现将 $x^*$ 分类为目标类 $t$ 的目标。

#### 解决
为了解决这个问题，观察到为了获得 $f(x) = t$，攻击者需要满足以下条件：

$$
g_t(x) \geq g_y(x) \quad \forall y \neq t.
$$

通过在此条件中添加一个安全边际，可以增加定向攻击的鲁棒性：

$$
g_t(x) - \delta \geq g_y(x) \quad \forall y \neq t. \quad (4.21)
$$

重写这个条件，我们得到：

$$
g_t(x) - g_y(x) \geq \delta \quad \forall y \neq t,
$$

即

$$
\max_{y \neq t} g_y(x) - g_t(x) \leq -\delta. \quad (4.21) 
$$

Carlini 和 Wagner [2017] 建议将目标问题 (4.19) 中的 $g_t(x)$ 替换为具有以下属性的函数 $h(x | t)$：$h(x | t) \leq 0$ 当且仅当条件 (4.21) 满足（即当 $f(x) = t$ 时，且有额外的边际）。他们实验中表现特别好的一个函数是：

$$
h(x | t) = \max\left\{0, \max_{y \neq t} (g_y(x) - g_t(x))\right\}.
$$

因此，我们可以将定向攻击的优化问题重写为：

$$
\min \left[h(x | t) + \lambda c(x, x_A)\right]; \quad (4.22)
$$

#### 可靠性攻击的变换。类似的变换

类似的变换可以为可靠性攻击设计。虽然得到的优化问题通常是非凸的，但可以应用标准方法，如梯度下降或局部搜索来求解这些问题 [Hoos 和 Stützle, 2004; Nocedal 和 Wright, 2006]。

对多类分类器的攻击在研究深度学习算法的鲁棒性时变得尤为重要。我们将在第八章中专门讨论这个主题。

## 4.3.3 决策时间攻击对异常检测器的影响 ANOMALY DETECTORS

一些知名的规避攻击并不是针对分类器，而是针对异常检测系统。==虽然异常检测和分类的具体方法通常存在重大差异（前者是无监督的，而后者是监督的）==，但实际上，对异常检测器的决策时间攻击与==对二元分类器的规避攻击基本相同。==

### 中心点基础的异常检测

为了理解这一点，考虑中心点基础的异常检测器，给定 centroid $\mu$。如第 2.2.4 节所讨论的，当特征向量 $x$ 满足 $\| x - \mu \| \leq r$ 时，被视为正常。现在，定义：

$$
g(x) = \| x - \mu \| - r.
$$

可以看出，当 $f(x) = \text{sgn}(g(x)) = 1$，或者等效地，当 $g(x) \geq 0$ 时，实例 $x$ 被分类为异常。因此，我们讨论的所有针对二元分类器的攻击都可以直接应用于此。

### PCA基础的异常检测

同样，对于基于主成分分析（PCA）的异常检测器，可以定义：

$$
g(x) = \| P x - r \| = \| (I - V V^T)x \| - r; \quad (4.24)
$$

其中 $V$ 是由 PCA 生成的特征向量矩阵，随后可以应用标准的二元分类器规避技术。

## 4.3.4 决策时间攻击对聚类模型的影响

正如对异常检测器的决策时间攻击在概念上与对二元分类器的规避攻击相等，聚类的决策时间攻击自然与多类分类器的攻击等价。

### 聚类模型的定义

我们通常将聚类模型表示为将整个特征空间 $X$ 划分为 $K$ 个子集 $\{S_1, \ldots, S_K\}$，对应于 $K$ 个聚类。一个任意特征向量 $x$ 属于聚类 $k$ 当且仅当 $x \in S_k$。通常，这种聚类分配可以表示为：

$$
k \in \arg \max_y g_y(x)
$$
#### 解释
对于某个 $g_y(x)$。例如，==假设分配是基于聚类均值的 $l_p$ 距离，==$\{\mu_1, \ldots, \mu_K\}$ 是一组聚类均值；常见的 k-means 聚类方法是具有 $p=2$ 的特殊情况。然后：

$$
k \in \arg \min_y \| x - \mu_y \|_p = \arg \max_y \| x - \mu_y \|_1.
$$

即

$$
g_y(x) = \| x - \mu_y \|_1.
$$

#### 定向攻击与可靠性攻击

考虑到这一点，我们现在可以定义聚类中的定向攻击和可靠性攻击。对于定==向攻击，攻击者旨在确保实例 $x_A$ 被错误分类为属于目标聚类 $t$==，其相关的聚类均值为 $\mu_t$。同样，我们可以定义一个==可靠性攻击，确保理想实例不再落入其原始（正确）聚类 $y$ 中。==

鉴于上述对 $g_y(x)$ 的定义，我们可以像在多类分类器中建模决策时间攻击一样，对聚类进行类似的建模。

## 4.3.5 决策时间攻击对回归模型的影响

在针对回归模型的决策时间攻击中，攻击者将从==理想特征向量 $x_A$ 开始，旨在将其转变为另一个向量 $x'$，以实现定向攻击或可靠性攻击。==
	在定向攻击中，攻击者有一个目标回归值 $t$，并希望使回归模型的预测值 $f(x')$ 尽可能接近 $t$。而在可靠性攻击中，攻击者的目标是使预测值 $f(x')$ 偏离正确预测 $y$。

### 示例
![[Pasted image 20241013212800.png]]

我们通过示例来说明==针对回归的决策时间攻击==
### 攻击建模

我们可以通过考虑==对抗损失函数 $l_A(f(x); t)$ 来建模定向攻击==，该函数从==攻击者的角度衡量对抗实例 $x$ 引入的误差，与攻击者的预测目标 $t$ 相关==。
	这使我们能够重新捕捉实现对抗目标与最小化攻击者在执行决策时间攻击时所面临的操控量之间的经典权衡：==权衡的前半部分由损失函数 $l_A$ 捕捉，而后半部分使用我们之前讨论的相同类型的成本函数==。
#### 决策时间定向攻击可以建模为以下优化问题

$$
\min l_A(f(x); t) + \lambda c(x, x_A); \quad (4.25)
$$

#### 可靠性攻击的对应形式为：

$$
\max l(f(x); y) - \lambda c(x, x_A); \quad (4.26)
$$

在这里，攻击者现在试图最大化学习者的损失函数 $l(f(x); y)$。对于损失和成本函数的常见特例是平方 $l_2$ 范数，即 $l_A(f(x); t) = \| f(x) - t \|^2$ 和 $c(x, x_A) = \| x - x_A \|^2$。

### 线性回归模型的攻击

以线性回归模型的攻击为例 。假设给定数据点 $(x_A, y)$ 和线性回归模型 $f(x) = w^T x$，攻击者旨在将预测偏向目标 $t$。
	
	如果我们对攻击者的损失和成本函数都使用（平方的）$l_2$ 范数，则攻击者的优化问题变为：
	
	$$
	\min (w^T x - t)^2 + \lambda \| x - x_A \|^2; \quad (4.27)
	$$
	
	现在，我们可以更一般地将该攻击应用于数据集 $\{(x_{iA}, y_i)\}$，与每个 $(x_{iA}, y_i)$ 相关的目标向量 $t$。我们可以将所有特征向量聚合到特征矩阵 $X_A$ 中，将所有标签聚合到标签向量 $y$ 中。我们可以将上述优化问题以矩阵形式表示，以查找从 $X_A$ 到新矩阵 $X$ 的最佳转换：
	
	$$
	\min (X_A w - t)^T (X_A w - t) + \lambda \| X - X_A \|_F; \quad (4.28)
	$$
	
	其中 $\| X - X_A \|_F$ 是 Frobenius 范数。我们可以使用一阶条件来表征攻击者对固定模型参数向量 $w$ 的最佳响应；因此，将一阶导数等于零，我们得到：
	
	$$
	X' w - t + X' = X_A; \quad (4.29)
	$$
	
	$$
	X' = (\lambda I + w w^T)^{-1} (t w^T + \lambda X_A); \quad (4.30)
	$$
	
	使用 Sherman-Morrison 公式，我们可以等效地写为：
	
	$$
	X' = X_A - \frac{(\lambda + \| w \|^2)}{(1 + \lambda)} (X_A w - t) w^T.
	$$
	
	因此，$X'$ 中的每一行 $i$ 都成为原始理想特征向量 $x_{iA}$ 的一种转化，变为攻击 $x_i'$。

### 对线性自回归linear autoregressive模型的攻击

Alfeld 等人 [2016] 提出了另一种有趣的回归模型攻击变体。特别地，他们考虑==对线性自回归linear autoregressive（AR）模型==的攻击，这些模型通常用于时间序列分析和预测，例如在金融市场中。
	具体而言，Alfeld 等人 [2016] 描述了对线性自回归模型的攻击，其形式为：
	
	$$
	x_j = \sum_{k=1}^{d} w_k x_{j-k} + \epsilon_j,
	$$
	
	其中 $x_j \in \mathbb{R}$ 是时间 $j$ 的标量观测值。在这个上下文中，防御者（学习者）的目标是在时间 $j$ 进行未来 $h$ 个时间步的预测，可以通过递归地应用上述方程实现。攻击者观察并可以修改 $d$ 个观测值。
	
	在正式的符号中，假设在任意时刻，防御者的 AR(d) 模型使用由 $x = (x_{-d}, \ldots, x_{-1})$ 表示的前 $d$ 个观测值，其中 $x_{-k}$ 指的是在决策时间点 $k$ 步之前的观测值（即如果决策点是时间 $j$，则对应于方程 (4.32) 中的 $x_{j-k}$）。攻击者可以对通知 AR 模型的观测值进行修改，从而产生损坏的观测值 $x'_k$。同时，修改观测值向量会产生成本 $c(x, x')$。
	
	如上所述，攻击者的目标可以是定向攻击或对学习者可靠性的攻击。我们以定向攻击为例，攻击者有一个目标预测向量 $t \in \mathbb{R}^h$，希望诱导。假设 $x_h$ 是防御者做出的 $h$ 步预测，则攻击者的相应损失为：
	
	$$
	\| x_h - t \|_W^2; \quad (4.33)
	$$
	
	其中 $\| u \|_W^2 = u^T W u$ 是马哈拉诺比斯范数。正如在典型的规避攻击模型中，自然地将成本建模为范数，例如平方 $l_2$ 距离：
	
	$$
	c(x, x') = \| x - x' \|^2; \quad (4.34)
	$$
	
	在这种情况下，我们可以等效地表示为 $x' = x + \delta$，其中某个攻击向量 $\delta$，成本对应于 $\| \delta \|^2$。
	
	为了显著简化该问题的分析，可以将其转化为矩阵-向量符号。首先，观察到 $x_{j+1} = S x_j$，其中 $S$ 是一步转移矩阵：
	
	$$
	S = \begin{pmatrix}
	0_{h-1} & I_{(h-1)*(h-1)} \\
	0 & 0^T_{h-d-1} & 1 & w^T
	\end{pmatrix}; \quad (4.35)
	$$
	
	因此，从当前时间 $j=0$ 开始到时间 $j=h-1$（即，经过 $h$ 个时间步）的预测向量为 $x_{h-1} = S^h x_{-1}$。在对抗性篡改 $\delta$ 后，我们得到篡改结果：
	
	$$
	x'_{h-1} = S^h (x_{-1} + Z \delta); \quad (4.36)
	$$
	
	其中
	
	$$
	Z = \begin{pmatrix}
	0_{h-d} \\
	I_d
	\end{pmatrix}. \quad (4.37)
	$$
	
	如上所述，我们可以考虑许多攻击模型变体，有和没有约束。一个简单的变体，允许闭合形式解，是攻击者解决以下优化问题：
	
	$$
	\min \| S^h (x_{-1} + Z \delta) - t \|^2_W + \lambda \| \delta \|^2; \quad (4.38)
	$$
	
	注意这与上述问题 (4.25) 的类比，其中 $l_A(f(x); t) = \| S^h (x_{-1} + Z \delta) - t \|^2_W$，而 $c(x, x_A)$ 由平方 $l_2$ 范数捕捉。最优解为：
	
	$$
	\delta^* = - (Q + 2 \lambda)^{-1} c; \quad (4.39)
	$$
	
	其中
	
	$$
	Q = (S^h Z)^T W (S^h Z); \quad (4.40)
	$$
	
	$$
	c = Z^T (S^h)^T W^T S^h x_{-1} - Z^T (S^h)^T W^T t; \quad (4.41)
	$$

# 4.4 黑箱决策时间攻击

==白箱决策时间攻击揭示了学习方法的潜在脆弱性==

在理解黑箱决策时间攻击时，有==两个核心问题：==

（1）如何对==攻击者可能拥有的关于系统的部分信息进行分类，以及利用这些信息能实现什么；==
（2）如何==建模攻击者获取信息的方式==

我们通过==描述黑箱决策时间攻击的全面分类法==来解决第一个问题。然后，我们==讨论一个自然的查询框架==，攻击者可以在该框架内获取有关学习模型的信息，重点讨论对抗性规避问题。

## 4.4.1 黑箱攻击的分类法

正如我们之前观察到的，决策时间攻击是针对学习模型 $f$ 的攻击。==因此，黑箱攻击从根本上讲是关于攻击者拥有或可以推断出的有关学习者使用的实际模型 $f$ 的信息。==

![[Pasted image 20241013215147.png]]
#### 解释
在图 4.3 中，我们展示了黑箱决策时间攻击的分类法的可视化。由于决策时间攻击是针对模型的攻击，因此分类法==围绕攻击者可能拥有的信息组织==，包括所使用的特征空间。这是我们分类法的组织原则。

在白箱攻击中，攻击者知道==特征空间 $F$ 和真实模型 $f$。==等效地，攻击者只需知道数据集 $D$ 及其特征集 $F$，然后通过对数据集 $D$ 应用学习算法 $A$ 运行来推导模型 $f$（假设 $A$ 是确定性的）。另一方面，如果 $F$、$D$ 和 $A$ 中的任何一个没有被准确知道，攻击者只能获得一个近似模型 $f_Q$，该模型近似真实目标模型 $f$。此外，近似模型 $f_Q$ 还可以表示对真实模型的查询访问，这一点将在下面详细讨论。

### 信息状态的分类

让我们从攻击者知道特征集 $F$ 开始。如果攻击者还知道数据集 $D$，他们可以使用代理算法推导出代理模型 $f_Q$。因此，==我们得到了信息状态 $\langle F, f_Q \rangle$（攻击者知道特征，并且有一个代理模型）==。

	我们一般认为这种方法在分类学习中非常有效：只要 $f$ 和 $f_Q$ 都是高度准确的，它们在期望上必然相似。正式地，假设 $h(x)$ 是 $f$ 和 $f_Q$ 都试图拟合的真实函数，并且假设 $f$ 和 $f_Q$ 的误差率都被界定在 $\epsilon$ 之内。那么，
	
	$$
	\Pr\{f(x) \neq f_Q(x)\} = \Pr\{[f(x) \neq h(x)] \land [f_Q(x) = h(x)]\} \lor [f(x) = h(x) \land f_Q(x) \neq h(x)].
	$$
	
	这可以进一步转化为：
	
	$$
	\Pr\{f(x) \neq h(x)\} + \Pr\{f_Q(x) \neq h(x)\} \leq 2\epsilon.
	$$

即使攻击者只有一个代理数据集 $D_Q$，他们仍然可以通过在 $D_Q$ 上运行与学习者相同的算法或一个代理算法来推导出代理模型 $f_Q$。因此，我们仍然得到信息状态 $\langle F, f_Q \rangle$。
	这种攻击的成功在很大程度上取决于 $D_Q$ 对真实数据 $D$ 的近似程度；例如，如果它们足够大，并且来自相似的实例分布，我们期望 $f_Q$ 仍然是模型 $f$ 的良好近似。

注意，即使攻击者不知道真实特征空间，他们也可以仅使用代理特征集 $F_Q$。
	无论他们是否知道数据或算法，攻击者仍然可以获得近似模型 $f_Q$，从而导致一个稍微低的信息状态 $\langle F_Q, f_Q \rangle$。然而，知道 $F$ 和 $F_Q$ 之间的差异是至关重要的：如果代理特征空间与 $F$ 的差异很大，则攻击成功的可能性可能会大大降低。

随着我们进一步向无信息的极端发展，攻击者可能仅对特征 $F$ 有所了解。
	在这种情况下，攻击者仍然可以执行模仿攻击：例如，给定一个原始恶意实例（如恶意软件）和一小部分良性实例，攻击者可以尝试直接操纵恶意实例，使其特征接近良性实例的特征。模仿攻击也可以使用代理特征集 $F_Q$ 进行，但在这里，如果 $F_Q$ 对 $F$ 的近似不够好，也不太可能成功。最后，攻击者可能对特征一无所知，但可能对真实模型有查询访问权限——我们也将此信息状态表示为 $f_Q$。在这种情况下，攻击者可能能够直接操纵恶意实例（例如，添加或删除恶意 PDF 文件中的对象），反复查询模型以确定修改是否足以绕过检测器。这就是 EvadeML 攻击的性质 [Xu et al., 2016]。

#### 小结

这种分类法为理解黑箱决策时间攻击提供了一个框架，帮助我们识别在不同攻击情境下，攻击者可能具备的信息和能力。这不仅能帮助设计更强的防御机制，还能促进对现有攻击方法的进一步研究和改进。

## 4.4.2 攻击者信息获取建模 INFORMATION ACQUISITION

黑箱攻击的早期研究提出了一种自然的查询模型：攻击者可以提交特征向量 $x$ 作为输入，并观察学习者分配的标签 $f(x)$（例如，分类器是否将 $x$ 视为恶意）。接下来，我们将讨论查询模型，==重点关注二元分类器的对抗性规避问题。==

1. **查询模型**：
   - 攻击者可以通过查询特征向量 $x$，获取学习者所做出的分类决策 $f(x)$，例如识别某个样本是否恶意。
   - 该查询模型使攻击者有机会通过提交特征向量来逐步了解分类器的行为。

2. **ACRE 可学习性**：ACRE Learnability (ACRE stands for adversarial classifier reverse engineering).
   - Lowd 和 Meek 提出了 ACRE（对抗性分类器反向工程）的概念，研究了如何通过多次查询来最小化被分类为良性样本的代价。
   - 他们证明，即使在简单的二元特征空间中，这个问题也是 NP-难的。

3. **反向工程问题**：
   - 攻击者的目标是通过有限的查询来推断出分类器的行为。这个过程涉及到创建一个代理模型 $f_Q$，来尽量接近真实模型 $f$。
   - 攻击者需要在误差 $\epsilon$ 内进行反向工程，这是通过查询和学习来实现的。

4. **有效学习和反向工程的关系**：
   - 定理表明，如果一个假设类是可学习的，那么攻击者可以有效地进行反向工程。
   - 这意味着，如果防御者的学习算法有效，攻击者也能够利用查询来推断出分类器的行为。

5. **实际操作步骤**：
   - 攻击者可以生成特征向量，查询分类器以获得标签，形成数据集，然后使用与防御者相同的学习算法来构建代理模型。

6. **局限性**：
   - 该方法假设攻击者了解防御者的学习算法和特征空间，使用代理算法和代理特征空间可能会降低攻击效果。

7. **分类分数的观察**：
   - 在另一种变体中，攻击者可以观察查询实例的分类分数，这通常比直接获取分类结果更容易进行学习。

## 4.4.3 使用近似模型进行攻击 APPROXIMATE MODEL

在黑箱攻击中，即使我们能够获得决策函数 $f(x)$ 或评分函数 $g(x)$ 的合理估计，==它也从未是完全正确的==。我们将评分函数的近似表示为 $g_Q(x)$。攻击者通常会希望在进行优化时使用 $f_Q(x)$ 或 $g_Q(x)$，但==为了确保攻击的成功率，攻击者可能会采取更保守的策略==，考虑模型的不确定性。

### 攻击策略

1. **不确定性规避**：
   - 在对抗性规避攻击中，攻击者可以通过引入一个项来评分修改实例 $x$ 与良性特征向量的相似性，从而对抗不确定性。
   - 成功的攻击应该在特征空间中==看起来与良性样本相似。==

2. **优化问题的修改**：
   - 假设 $p_b(x)$ 表示良性类特征向量的密度函数，攻击者的优化问题可以如下修改（以问题 (4.6) 为例）：
   $$
   \min g_Q(x) + \lambda c(x, x_A) - \beta p_b(x),
   $$
   其中：
   - $g_Q(x)$ 是近似评分函数。
   - $c(x, x_A)$ 表示修改实例的成本。
   - $p_b(x)$ 是良性数据的密度函数。

   这个优化目标==不仅考虑攻击目标==（通过 $g_Q(x)$ 捕获）和成本（通过 $c(x, x_A)$ 捕获），还==试图最大化 $x$ 被判定为良性的可能性==，基于良性数据的密度 $p_b(x)$。

   - 自然地，攻击者会想知道如何获得或近似密度函数 $p_b(x)$。Biggio 等人 [2013] 提出的一个方法是通过应用基于核的非参数密度估计，使用已知良性实例的数据集 $D^*$，公式如下：
   $$
   p_b(x) = \sum_{i \in D^*} k(x - x_i),
   $$
   其中 $k(\cdot)$ 是核函数，$h$ 是核平滑参数。

